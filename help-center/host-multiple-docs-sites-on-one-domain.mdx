---
title: "Can I host multiple docs sites on the same domain?"
description: "One custom domain connects to one Mintlify deployment. Combine products into a single site with path-based navigation or use separate subdomains."
keywords: ["multiple docs sites", "same domain", "multi-product documentation", "custom domain", "one deployment per domain", "subdomain per product", "consolidate docs sites"]
---

A custom domain connects to exactly one Mintlify deployment. You cannot point `example.com/product-a` at one deployment and `example.com/product-b` at another. If you try to add a domain that another deployment already holds, the dashboard returns a `Domain is already in use by another deployment in your organization` or `Domain is already claimed by another organization` error. See [Fix "Domain already claimed by another organization" error](/help-center/domain-already-claimed-by-another-organization).

To document multiple products, choose one of these patterns.

## One site with path-based navigation

Combine all products into a single deployment and separate them with navigation. Each product gets its own URL path on the shared domain, like `docs.example.com/product-a` and `docs.example.com/product-b`.

- Use [products](/organize/navigation#products), [tabs](/organize/navigation#tabs), or [dropdowns](/organize/navigation#dropdowns) in your `docs.json` to give each product a dedicated navigation division.
- If each product's documentation lives in its own Git repository, use a [multi-repository deployment](/deploy/multi-repo) to combine the repositories into one site with a URL path per repository. Multi-repository deployments are available on Enterprise plans.

This pattern keeps all products searchable from one place and requires only one custom domain.

## Separate sites on separate subdomains

Keep each product as its own deployment and give each deployment its own subdomain, like `docs-product-a.example.com` and `docs-product-b.example.com`. Add each subdomain as the [custom domain](/customize/custom-domain) for its deployment.

Use this pattern when products need fully independent sites, such as separate teams, themes, or release processes.

## Consolidate existing sites

If you move separate sites into one deployment:

1. Move the content into the combined deployment and organize it with navigation.
2. Add [redirects](/create/redirects) in `docs.json` for any page paths that changed. Redirects only apply within the deployment that defines them.
3. Remove the custom domain from each retired deployment so the domain is released. See [Remove a custom domain](/customize/custom-domain#remove-a-custom-domain).
4. Redirect traffic from retired subdomains at your DNS provider or hosting layer. Mintlify cannot redirect a domain that is no longer connected to a deployment.
