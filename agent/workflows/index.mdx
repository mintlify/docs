---
title: "Workflows"
description: "Automate documentation tasks with the Mintlify agent on a schedule or in response to repository events."
keywords: ["automation", "automate", "cron", "agent", "workflow"]
tag: "Beta"
---

<Info>
  Workflows are in beta and are currently free for all plans during the beta period. Features, availability, and pricing are subject to change.
</Info>

Workflows run the Mintlify agent automatically on a schedule or in response to pushes to a repository. Each workflow defines a prompt and a trigger. When it runs, the agent clones any specified repositories as context, follows the prompt, and opens a pull request (or merge request on GitLab) with the resulting changes.

## How workflows run

When a workflow triggers, Mintlify:

1. Queues a workflow run.
2. Provisions an isolated sandbox environment.
3. Clones your documentation repository and any context repositories into the sandbox.
4. Sends your prompt to the agent.
5. The agent makes changes, then opens a pull request against your deployment branch—or merges it automatically if automerge is enabled.

Runs that fail do not count toward usage limits.

## Trigger types

Workflows support two trigger types:

- **Schedule (cron)** — Run on a recurring UTC schedule. Workflows queue within 10 minutes of the scheduled time.
- **Push events** — Run when changes push to a specific repository or branch, including pull request merges and direct pushes.

Each workflow has exactly one trigger. You cannot combine trigger types in a single workflow.

## Agent environment

The agent runs in an isolated sandbox with restricted internet access. Package registries and most external services are not reachable. When writing prompts, only reference tools available in the sandbox:

- Standard shell utilities (`grep`, `sed`, `awk`, `curl`, and others)
- `git` and the GitHub CLI (`gh`)
- The Mintlify CLI (`mint`)
- Node.js and Bun

The agent cannot install additional packages or tools at runtime. Prompts that reference unavailable tools may produce unexpected results or fail.

## Usage limits

Each workflow can run up to 50 times per day. Hobby plan projects have an additional limit of 100 runs per month. Runs that fail do not count toward either limit.

## Next steps

- [Create a workflow](/agent/workflows/create)
- [Manage workflows](/agent/workflows/manage)
- [Write effective prompts](/agent/workflows/prompts)
- [Configuration reference](/agent/workflows/reference)
