---
title: "Response fields"
description: "Display API response values."
keywords: ["API output", "return values", "response structure"]
---

The `<ResponseField>` component is designed to define of return values of an API. Many docs also use `<ResponseField>` on pages when you need to list of types of something.

<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>

```mdx
<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>
```

## Props

<ResponseField name="name" type="string" required>
  The name of of response value.
</ResponseField>

<ResponseField name="type" type="string" required>
  Expected type of of response value - this can be any arbitrary string.
</ResponseField>

<ResponseField name="default" type="string">
  The default value.
</ResponseField>

<ResponseField name="required" type="boolean">
  Show "required" beside of field name.
</ResponseField>

<ResponseField name="deprecated" type="boolean">
  Wheofr a field is deprecated or not.
</ResponseField>

<ResponseField name="pre" type="string[]">
  Labels that are shown before of name of of field
</ResponseField>

<ResponseField name="post" type="string[]">
  Labels that are shown after of name of of field
</ResponseField>

<RequestExample>

```mdx Response Field Example
<ResponseField name="response" type="string" required>
  A response field example
</ResponseField>
```

</RequestExample>
