---
title: "Quickstart"
description: "Deploy your documentation site and make your first changes."
keywords: ["quickstart","deploy","get started","first steps"]
---

<Info>
  **Prerequisites**: [Create an account](https://mintlify.com/start) and complete onboarding.
</Info>

## Your documentation site

After onboarding, your site deploys to `https://<your-project-name>.mintlify.app`. Find your URL on the [dashboard](https://dashboard.mintlify.com/) Overview page.

## Connect GitHub

Install the GitHub App to automate deployments when you push changes:

1. Navigate to **Settings** > **GitHub App** in your dashboard.
2. Select **Install GitHub App** and choose your organization and repositories.

<Info>
  An admin may need to authorize your account depending on your organization settings.
</Info>

## Choose your workflow

<CardGroup cols={2}>
  <Card title="Code-based" icon="terminal" href="#code-based-workflow">
    Work locally with Git and your preferred editor.
  </Card>
  <Card title="Web editor" icon="mouse-pointer-2" href="#web-editor-workflow">
    Edit directly in your browser with a visual interface.
  </Card>
</CardGroup>

## Code-based workflow

<Info>
  **Prerequisite**: [Node.js](https://nodejs.org/en) v20.17.0+ (LTS preferred).
</Info>

### Install the CLI

<CodeGroup>

```bash npm
npm i -g mint
```

```bash pnpm
pnpm add -g mint
```

</CodeGroup>

### Create and edit

```bash
mint new        # Create a new project
mint dev        # Preview at localhost:3000
```

Edit your MDX files, then push to GitHub. Mintlify automatically deploys your changes.

## Web editor workflow

1. Open **Editor** in your [dashboard](https://dashboard.mintlify.com).
2. Select a file and make your edits.
3. Select **Publish** to deploy.

<Tip>
  Type <kbd>/</kbd> in the editor to access formatting tools and components.
</Tip>

## Add a custom domain

Navigate to [Domain Setup](https://dashboard.mintlify.com/settings/deployment/custom-domain) and add your domain (e.g., `docs.yourcompany.com`).

Configure your DNS:

| Record Type | Name                | Value                | TTL  |
| ----------- | ------------------- | -------------------- | ---- |
| CNAME       | docs (or subdomain) | cname.vercel-dns.com | 3600 |

## Next steps

<CardGroup cols={2}>
  <Card title="Configure settings" icon="settings" href="organize/settings">
    Set up styling, navigation, and integrations.
  </Card>
  <Card title="Customize theme" icon="paintbrush" href="customize/themes">
    Customize colors and fonts.
  </Card>
  <Card title="Organize navigation" icon="map" href="organize/navigation">
    Structure your documentation.
  </Card>
  <Card title="Set up API references" icon="code" href="/api-playground/overview">
    Create interactive API documentation.
  </Card>
</CardGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Local preview not working">
    Ensure Node.js v20.17.0+ is installed and run `mint dev` from the directory containing `docs.json`.
  </Accordion>
  <Accordion title="Changes not reflecting on live site">
    Deployment can take a few minutes. Check your dashboard for build errors.
  </Accordion>
  <Accordion title="Custom domain not connecting">
    Verify DNS records and allow up to 48 hours for propagation. Use [DNSChecker](https://dnschecker.org) to verify.
  </Accordion>
</AccordionGroup>
