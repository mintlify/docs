---
title: "Plausible"
---

Add your site's domain (without `http://` or `https://`) to `mint.json` for Plausible analytics:

<CodeGroup>

```json mint.json
"analytics": {
    "plausible": {
        "domain": "docs.domain.com"
    }
}
```

</CodeGroup>
</fileContent>