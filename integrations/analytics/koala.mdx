---
title: "Koala"
description: "Track visitor behavior and identify high-intent accounts with Koala analytics."
keywords: ["Koala", "intent signals", "account identification", "visitor tracking", "Koala public API key"]
---

Add the following to your `docs.json` file to send analytics to Koala.

<CodeGroup>

```json Analytics options in docs.json
"integrations": {
    "koala": {
        "publicApiKey": "required"
    }
}
```

```json Example
"integrations": {
    "koala": {
        "publicApiKey": "pk_abc123def456"
    }
}
```

</CodeGroup>

Replace `pk_abc123def456` with your Koala public API key. You can find this in your [Koala workspace settings](https://app.getkoala.com/goto/settings/install).
