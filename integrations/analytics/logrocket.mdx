---
title: "LogRocket"
description: "Monitor user sessions and errors with LogRocket by adding your API key to the docs.json integrations configuration."
keywords: ["LogRocket integration", "session replay", "error monitoring", "app ID"]
---

Add the following to your `docs.json` file to send analytics to LogRocket.

```json Analytics options in docs.json
"integrations": {
    "logrocket": {
        "apiKey": "required"
    }
}
```
