---
title: "Mixpanel"
---

Add the following to your `docs.json` file to send insights to Mixpanel.

```json Insights options in docs.json
"integrations": {
    "mixpanel": {
        "projectToken": "required"
    }
}
```
