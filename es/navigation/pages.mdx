---
title: Páginas y Grupos
---

## Páginas

Si no quieres ninguna jerarquía, puedes simplemente definir páginas dentro de tu campo `navigation`.

Cada entrada del arreglo `pages` debe ser una ruta a un archivo que exista dentro de tu repositorio.
Nota que no necesitas agregar `.mdx` a las rutas de archivo.

```json
{
  "navigation": {
    "pages": [
      "overview",
      "quickstart",
      "advanced/components",
      "advanced/integrations"
    ]
  }
}
```

## Grupos

Los Grupos te permiten agrupar tus páginas. Los grupos también pueden anidarse entre sí.

```json
{
  "navigation": {
    "groups": [
      {
        "group": "Getting Started",
        "pages": [
          "quickstart",
          {
            "group": "Editing",
            "icon": "pen-paintbrush",
            "pages": ["development", "web-editor"]
          }
        ]
      },
      {
        "group": "Writing Content",
        "pages": ["writing-content/page", "writing-content/text"]
      }
    ]
  }
}
```
