---
{}
---

<Note>
  Si actualmente está usando el archivo de configuración heredado `mint.json`, por favor actualice el CLI:

  ```sh
  npm i -g mintlify@latest
  ```

  Y ejecute el nuevo `upgrade` comando en su repositorio de documentación:

  ```sh
  mintlify upgrade
  ```

  Esto generará un `docs.json` basado en su `mint.json`. Luego, por favor elimine el `mint.json` archivo de su repositorio.
</Note>
