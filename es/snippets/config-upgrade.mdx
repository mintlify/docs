---
{}
---

<Note>
  Si actualmente estás usando el archivo de configuración heredado `mint.json`, por favor actualiza el CLI:

  ```sh
  npm i -g mintlify@latest
  ```

  Y ejecuta el nuevo `upgrade` comando en tu repositorio de documentación:

  ```sh
  mintlify upgrade
  ```

  Esto generará un `docs.json` basado en tu `mint.json`. Luego, por favor elimina el archivo `mint.json` de tu repositorio.
</Note>
