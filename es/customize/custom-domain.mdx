---
title: "Dominio personalizado"
description: "Aloja tu documentación en el dominio personalizado de tu sitio web"
og:description: "Aloja tu documentación en el dominio personalizado de tu sitio web"
---

Para alojar tu documentación en un dominio personalizado, establece el dominio que desees en la configuración de Mintlify y configura los ajustes de DNS en tu proveedor de dominio.

<Info>
  ¿Quieres configurar un subdirectorio personalizado como `mintlify.com/docs`? Consulta la documentación sobre el [subdirectorio personalizado](/es/deploy/cloudflare).
</Info>

<div id="dashboard-settings">
  ## Configuración del dashboard
</div>

1. Ve a tu [dashboard](https://dashboard.mintlify.com).
2. Selecciona **Settings**.
3. Selecciona **Domain Setup**.
4. Ingresa el dominio personalizado que desees. Por ejemplo, `docs.mintlify.com`.

<Frame>
  <img alt="La página de configuración de Custom Domain con el campo Enter your domain URL resaltado." className="block dark:hidden" src="/images/add-custom-domain-light.png" />

  <img alt="La página de configuración de Custom Domain con el campo Enter your domain URL resaltado." className="hidden dark:block" src="/images/add-custom-domain-dark.png" />
</Frame>

<div id="configure-your-dns">
  ## Configura tu DNS
</div>

1. Ve a la configuración de DNS de tu dominio en el sitio web de tu proveedor de dominio.
2. Crea una nueva entrada de DNS con los siguientes valores:

```bash
CNAME | docs | cname.vercel-dns.com.
```

<Frame>
  <img alt="Ejemplo de configuración de CNAME para un dominio personalizado alojado en Vercel." src="https://mintlify-assets.b-cdn.net/mintlify-dns.png" />
</Frame>

<div id="provider-specific-settings">
  ## Configuraciones específicas del proveedor
</div>

<AccordionGroup>
  <Accordion title="Verificación de Vercel">
    Si Vercel es tu proveedor de domain, debes agregar un registro `TXT` de verificación. Esta información aparece en tu dashboard después de enviar tu domain personalizado y también se te envía por correo electrónico.
  </Accordion>

  <Accordion title="Configuración de seguridad de Cloudflare">
    Si Cloudflare es tu proveedor de DNS, debes habilitar la opción de seguridad “full strict” para la configuración de HTTPS.
  </Accordion>
</AccordionGroup>

<div id="set-a-canonical-url">
  ## Establece una URL canónica
</div>

Después de configurar tu DNS, establece una URL canónica para asegurarte de que los motores de búsqueda indexen tu dominio preferido. Una URL canónica indica a los motores de búsqueda qué versión de tu documentación es la principal. Esto mejora el SEO cuando tu documentación es accesible desde varias URL y evita problemas de contenido duplicado.

Agrega la metaetiqueta `canonical` a tu `docs.json`:

```json
"seo": {
    "metatags": {
        "canonical": "https://www.tu-dominio-personalizado-aqui.com"
    }
}
```

Reemplaza `https://www.your-custom-domain-here.com` por tu domain personalizado real. Por ejemplo, si tu domain personalizado es `docs.mintlify.com`, usarías:

```json
"seo": {
    "metatags": {
        "canonical": "https://docs.mintlify.com"
    }
}
```
