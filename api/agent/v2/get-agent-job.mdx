---
title: "Get Mintie bot job"
description: "Retrieve the status and details of a Mintie bot job, including progress and pull request link."
openapi: /admin-openapi.json GET /v2/agent/{projectId}/job/{id}
keywords: ["Mintie bot job", "status", "retrieve", "details", "poll"]
---

Poll this endpoint to track the progress of a Mintie bot job. The `status` field transitions through `active` → `completed` or `failed`.

Once Mintie bot creates a pull request, the `prLink` field is populated.
