---
title: "Send follow-up message"
description: "Send a follow-up message to an existing Mintie bot job to provide additional instructions."
openapi: /admin-openapi.json POST /v2/agent/{projectId}/job/{id}/message
keywords: ["Mintie bot job", "message", "follow-up", "send"]
---

Send a follow-up instruction to an existing Mintie bot job. The message is processed asynchronously — poll the [get Mintie bot job](/api/agent/v2/get-agent-job) endpoint to track progress.

## Rate limits

- 100 uses per Mintlify project per hour

## Usage

```bash
curl -X POST https://api.mintlify.com/v2/agent/{projectId}/job/{id}/message \
  -H "Authorization: Bearer mint_xxxxx" \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Also add error handling examples to the quickstart guide"}'
```
