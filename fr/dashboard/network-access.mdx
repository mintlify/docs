---
title: "Accès réseau"
description: "Restreignez l'accès au dashboard et à l'éditeur Mintlify à des adresses IP et des plages CIDR de confiance avec une liste d'IP autorisées."
keywords: ["liste d'IP autorisées", "accès réseau", "CIDR", "restrictions IP", "sécurité", "accès au dashboard", "IP de confiance"]
---

<Info>
  Les contrôles d'accès réseau nécessitent un [plan Enterprise](https://mintlify.com/pricing?ref=network-access).
</Info>

Utilisez une liste d'IP autorisées pour restreindre l'accès au dashboard et à l'éditeur à des adresses IP et des plages CIDR de confiance. Lorsque vous activez la liste d'autorisation, les membres de l'organisation peuvent se connecter et utiliser le dashboard et l'éditeur web uniquement depuis une adresse IP autorisée.

La liste d'IP autorisées s'applique à votre dashboard et à votre éditeur Mintlify. Elle n'affecte pas votre site de documentation publié. Les requêtes API utilisent des contrôles d'IP distincts.

Vous pouvez autoriser l'accès de deux façons :

- **Adresse IP individuelle** : autorisez une seule adresse, telle que `203.0.113.7`.
- **Plage CIDR** : autorisez un bloc d'adresses, tel que `10.0.0.0/8`. Utilisez la notation CIDR pour accorder l'accès à un bureau entier ou à un réseau VPN sans lister chaque adresse individuellement.

<div id="configure-the-ip-allowlist">
  ## Configurer la liste d'IP autorisées
</div>

<Warning>
  Ajoutez votre adresse IP actuelle à la liste d'autorisation avant de l'activer. Si votre adresse ne figure pas dans la liste, vous risquez de vous verrouiller hors du dashboard. La page [Network access](https://app.mintlify.com/settings/organization/network-access) affiche votre adresse IP actuelle pour vous aider à éviter cela.
</Warning>

1. Dans votre dashboard Mintlify, accédez à la page [Network access](https://app.mintlify.com/settings/organization/network-access).
2. Sous **Allowed IP addresses**, saisissez une adresse IP individuelle (par exemple, `203.0.113.7`) ou une plage CIDR (par exemple, `10.0.0.0/8`).
3. Cliquez sur **Add**.
4. Répétez pour chaque adresse ou plage que vous souhaitez autoriser.
5. Activez la liste d'autorisation pour commencer à l'appliquer.
