---
title: "Utiliser l'agent IA de l'éditeur pour modifier des pages"
description: "Découvrez comment utiliser l'agent IA de l'éditeur pour modifier des pages, actualiser la navigation, exécuter bash et gérer les pages privées."
keywords: ["editor", "AI", "assistant", "agent", "chat", "code mode", "file upload"]
---

L'éditeur inclut un agent intégré qui peut modifier des pages, réorganiser la navigation, mettre à jour `docs.json`, effectuer des recherches dans l'ensemble de votre référentiel et gérer les paramètres du tableau de bord.

L'agent de l'éditeur effectue les modifications directement sur votre branche actuelle.

<div id="open-the-agent">

## Ouvrir l'agent

</div>

Cliquez sur **Ask agent** dans la barre d'outils de l'éditeur, ou appuyez sur <kbd>Cmd</kbd> + <kbd>I</kbd> (macOS) ou <kbd>Ctrl</kbd> + <kbd>I</kbd> (Windows).

Pour fermer le panneau, cliquez sur le bouton **X** dans l'en-tête du panneau ou appuyez à nouveau sur <kbd>Cmd</kbd> + <kbd>I</kbd>.

<div id="send-a-message">

## Envoyer un message

</div>

Saisissez votre requête dans la zone de chat en bas du panneau et appuyez sur <kbd>Enter</kbd> ou cliquez sur le bouton d'envoi <Icon icon="arrow-up" />.

Tapez <kbd>@</kbd> pour mentionner une page spécifique. L'agent se concentre sur cette page lors du traitement de votre requête. Si vous ne mentionnez pas de page, l'agent utilise la page actuellement ouverte.

Exemples de prompts :

- `simplify the introduction page`
- `fix all grammar errors across my content`
- `add a new page that explains authentication`
- `rename every mention of "Acme Pro" to "Acme Team" across all pages`
- `update docs.json to add a new group called "Guides"`

<div id="attach-files">

## Joindre des fichiers

</div>

Cliquez sur l'icône en forme de trombone dans la zone de chat, ou faites glisser-déposer des fichiers dans le panneau. L'agent lit les fichiers joints comme contexte pour votre requête.

Types de fichiers pris en charge :
- **Images** : JPG, PNG, GIF, WebP, SVG
- **Documents** : PDF
- **Code et texte** : `.js`, `.ts`, `.jsx`, `.tsx`, `.mdx`, `.md`, `.json`, `.yaml`, `.html`, `.css`, Python, Go, Rust, Ruby, Java, Swift, C, C++, SQL, scripts shell, et plus encore

Taille maximale : 5 Mo par fichier. Maximum : 10 fichiers par message.

<div id="add-a-selection-to-the-agent">

## Ajouter une sélection à l'agent

</div>

En mode visuel, sélectionnez du texte sur une page. Une barre d'outils flottante apparaît au-dessus de la sélection. Cliquez sur **Add to agent** pour envoyer le texte sélectionné à l'agent comme contexte.

<div id="review-what-the-agent-changed">

## Examiner ce que l'agent a modifié

</div>

À mesure que l'agent modifie vos pages, un panneau **Changed files** apparaît dans le chat. Développez-le pour voir la liste de tous les fichiers modifiés au cours de la session. Cliquez sur n'importe quel fichier pour l'ouvrir en vue de comparaison et comparer les modifications de l'agent à l'original.

<div id="what-the-agent-can-do">

## Ce que l'agent peut faire

</div>

<div id="edit-pages">

### Modifier des pages

</div>

L'agent peut écrire, réécrire, développer et réorganiser le contenu de n'importe quelle page. Il lit votre contenu existant pour respecter votre style et votre structure.

<div id="search-and-navigate-your-content">

### Rechercher et naviguer dans votre contenu

</div>

L'agent peut effectuer une recherche dans l'ensemble de votre référentiel, et pas seulement sur la page ouverte. Utilisez-le pour trouver des informations, repérer des incohérences ou vérifier qu'un contenu existe avant de l'ajouter.

<div id="update-navigation-and-docs-json">

### Mettre à jour la navigation et docs.json

</div>

L'agent peut ajouter, renommer, réordonner et supprimer des éléments de navigation, les mêmes modifications que vous feriez manuellement dans le panneau de navigation. Il peut aussi mettre à jour la configuration `docs.json` directement, y compris en ajoutant de nouveaux groupes, en ajustant des paramètres et en configurant des redirections.

Exemple : `add a "Quickstart" group under the Getting Started tab and move the quickstart page into it`

<div id="run-bash-commands">

### Exécuter des commandes bash

</div>

L'agent peut exécuter `grep`, `rg` et d'autres commandes bash sur votre référentiel. Utilisez cette fonction pour des opérations en masse sur de nombreux fichiers.

Exemple : `find every page that mentions the deprecated /v1/auth endpoint`

<div id="work-with-private-pages">

### Travailler avec des pages privées

</div>

Si vous avez accès aux pages privées, l'agent peut les lire et les modifier depuis la racine des pages privées. Utilisez cette fonctionnalité lorsque vous devez examiner ou mettre à jour du contenu qui ne doit pas apparaître dans votre navigation publique.

Vous pouvez ouvrir l'agent depuis l'éditeur de pages privées. Le bouton de l'agent est masqué pour les utilisateurs ayant le rôle de viewer. La navigation privée s'actualise lorsque l'agent ou un autre éditeur modifie l'arborescence des pages privées.

Vous pouvez également cliquer avec le bouton droit sur l'en-tête de la section **Private** dans la barre latérale pour créer une page ou un dossier privé si vous disposez des autorisations nécessaires.

Lorsque l'agent utilise une page privée, les libellés de ses outils affichent le titre de la page au lieu du nom de fichier basé sur l'UUID.

Lorsque vous ouvrez une page privée, la barre d'outils de l'éditeur affiche le libellé **Edited** et indique depuis combien de temps la page a été modifiée.

Les pages privées utilisent des noms de fichiers basés sur des UUID. Commencez par lister les pages privées, puis lisez ou modifiez le fichier dont vous avez besoin :

```bash
ls /home/daytona/private
cat /home/daytona/private/private-page-<uuid>.mdx
```

Dans le composeur, saisissez <kbd>@</kbd> pour rechercher une page privée par son titre. Les pages privées apparaissent dans le sélecteur de mentions avec le libellé `Private page`. Après sélection, sa puce ne contient pas de lien vers la page privée. L'agent résout la mention vers son chemin UUID et ne prend en compte que les pages auxquelles vous avez accès.

Pour créer un dossier privé, donnez-lui un titre humain comme dernier segment du chemin. Après une nouvelle liste, le dossier apparaît sous un chemin `private-folder-<uuid>` :

```bash
mkdir "/home/daytona/private/Onboarding"
ls /home/daytona/private
# private-folder-<uuid>
```

Pour réorganiser une page ou un dossier, `mv` peut le reparenter dans un autre dossier sous `/home/daytona/private` :

```bash
mv /home/daytona/private/private-page-<page-uuid>.mdx \
  /home/daytona/private/private-folder-<folder-uuid>/
mv /home/daytona/private/private-folder-<source-uuid> \
  /home/daytona/private/private-folder-<destination-uuid>/
```

Le déplacement d'une page de l'espace de travail vers `/home/daytona/private` n'est pas pris en charge.

Cette opération exige l'accès **Manager**. Elle conserve le nom UUID de l'élément ; `mv` ne renomme pas les pages et ne peut pas déplacer un dossier dans lui-même.

Pour copier une page privée, utilisez `cp` avec un nouveau titre humain comme dernier segment du chemin de destination :

```bash
cp /home/daytona/private/private-page-<source-uuid>.mdx \
  "/home/daytona/private/Copy of this page"
ls /home/daytona/private
# private-page-<new-uuid>.mdx
```

La copie est une nouvelle page privée et apparaît sous un chemin `private-page-<uuid>.mdx` après la prochaine liste. La copie de dossiers n'est pas prise en charge, pas plus que les copies entre les racines `/home/daytona/private` et `/home/daytona/workspace`.

Vous pouvez également supprimer une page privée si votre rôle vous y autorise. Un accès Manager est requis pour la suppression. Pour rendre une page privée publique, déplacez-la dans l'espace de travail :

```bash
mv /home/daytona/private/private-page-<uuid>.mdx /home/daytona/workspace/<path>.mdx
```

Le déplacement ajoute la page à la navigation publique et révoque son accès en tant que page privée. Vous n'avez pas besoin de configurer un autre système de fichiers ou une autre autorisation. L'agent utilise automatiquement vos autorisations existantes pour les pages privées.

<div id="configure-your-site-code-mode">

### Configurer votre site (mode code)

</div>

Pour les requêtes qui vont au-delà de la modification de pages (configurer l'authentification, gérer des workflows ou modifier les paramètres de déploiement), l'agent passe en mode code. Il écrit et exécute un script sur votre tableau de bord Mintlify en votre nom.

Le mode code respecte vos permissions du tableau de bord. Si vous n'avez pas accès à un paramètre dans le tableau de bord, l'agent ne peut pas non plus le modifier.

Exemples de prompts qui utilisent le mode code :

- `enable JWT authentication for my site`
- `create a workflow that updates my site when I merge a PR`
- `add a custom domain`

<div id="continue-an-automation-run">

### Poursuivre une exécution d'automatisation

</div>

Lorsque vous ouvrez l'éditeur depuis une exécution d'automatisation terminée en cliquant sur **View changes**, le panneau de l'agent s'ouvre automatiquement avec le contexte de ce que l'automatisation a fait. Une carte **Pages modifiées** en haut du chat liste chaque page modifiée par l'automatisation. Cliquez sur une page pour ouvrir une vue de diff.

L'agent dispose du contexte du prompt de l'automatisation, d'un résumé des modifications qu'elle a effectuées et des pages qu'elle a modifiées. Vous pouvez lui demander d'affiner ou d'étendre le travail de l'automatisation sans avoir à réexpliquer le contexte.

Par exemple : `The new section on rate limits is too long. Trim it to three sentences.`

<div id="session-history">

## Historique des sessions

</div>

Cliquez sur l'icône d'horloge dans l'en-tête du panneau pour voir vos précédentes sessions de chat. Cliquez sur une session pour la rouvrir et voir ce que l'agent a modifié.

<div id="ai-instructions">

## Instructions d'IA

</div>

Pour donner à l'agent des consignes persistantes telles que des règles de voix, de terminologie ou des conventions de mise en forme, configurez des instructions d'IA dans les [Paramètres de l'éditeur](/fr/editor/settings#ai-instructions). L'agent suit ces instructions à chaque requête sans que vous ayez besoin de les répéter.
