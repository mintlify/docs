---
title: "Authentification"
description: "Vous pouvez configurer des paramètres d’authentification pour permettre aux utilisateurs d’utiliser leurs clés d’API réelles."
---



<div id="enabling-authentication">
  ## Activation de l’authentification
</div>

Vous pouvez ajouter une méthode d’authentification à votre `docs.json` pour l’activer globalement sur chaque page, ou la définir au cas par cas.

La méthode d’authentification définie sur une page prévaut sur la méthode globale si les deux sont configurées.

<div id="bearer-token">
  ### Jeton Bearer
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "bearer"
      }
    }
}
```

```mdx Page Metadata
---
title: "Titre de votre page"
authMethod: "bearer"
---
```

</CodeGroup>

<div id="basic-authentication">
  ### Authentification Basic
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "basic"
      }
    }
}
```

```mdx Page Metadata
---
title: "Titre de votre page"
authMethod: "basic"
---
```

</CodeGroup>

<div id="api-key">
  ### Clé d’API
</div>

<CodeGroup>

```json docs.json
"api": {
    "mdx": {
      "auth": {
        "method": "key",
        "name": "x-api-key"
      }
    }
}
```

```mdx Page Metadata
---
title: "Titre de votre page"
authMethod: "key"
---
```

</CodeGroup>

<div id="none">
  ### Aucune
</div>

La méthode d’authentification "none" est utile pour désactiver l’authentification sur un endpoint spécifique après avoir défini une valeur par défaut dans docs.json.

<CodeGroup>
```mdx Page Metadata
---
title: "Titre de votre page"
authMethod: "none"
---
```
</CodeGroup>
