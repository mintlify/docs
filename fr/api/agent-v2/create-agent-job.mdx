---
title: "Créer une tâche agent (v2)"
description: "Créer une tâche agent qui s'exécute en arrière-plan et renvoie immédiatement un statut pour le polling."
openapi: /admin-openapi-v2.json POST /agent/{projectId}/job
keywords: ["tâche agent", "créer", "automatisation", "v2", "async"]
---

Cet endpoint crée une tâche agent qui s'exécute en arrière-plan. Contrairement à l'API v1 qui diffuse les réponses, l'API v2 renvoie immédiatement un ID de tâche que vous pouvez utiliser pour interroger le statut.

## Différences clés avec v1

- **Fire-and-forget** : La requête renvoie immédiatement l'ID de la tâche
- **Basé sur le polling** : Utilisez [Obtenir une tâche agent](/fr/api/agent-v2/get-agent-job) pour vérifier le statut et récupérer le lien PR
- **Requête simplifiée** : Utilise une seule chaîne `prompt` au lieu d'un tableau de messages
- **Conversationnel** : Envoyez des messages de suivi avec [Envoyer un message](/fr/api/agent-v2/send-message)

## Workflow

1. Créez une tâche avec cet endpoint
2. Stockez l'`id` retourné
3. Interrogez l'endpoint [Obtenir une tâche agent](/fr/api/agent-v2/get-agent-job) jusqu'à ce que le `status` soit `completed` ou `failed`
4. Récupérez le `prLink` de la réponse une fois la PR créée

## Limites de taux

L'API agent a les limites suivantes :

- 100 utilisations par projet Mintlify par heure
