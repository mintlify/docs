---
title: "Redirections et liens brisés"
description: "Outils pour éviter les liens invalides"
icon: "link-2"
---

Lorsque vous modifiez le chemin d’un fichier dans votre dossier de documentation, le chemin d’URL de cette page change également. Cela peut arriver lors de la restructuration de votre documentation ou de la modification du titre de la barre latérale.

## Liens brisés

Détectez les liens brisés avec notre CLI. [Installez la CLI](/fr/installation) et exécutez la commande :

```bash
mint broken-links
```

La CLI repère tous les liens relatifs dans votre documentation qui pointent vers des ressources inexistantes.

## Redirections

Configurez des redirections 301 en ajoutant le champ `redirects` à votre fichier `docs.json`.

```json
"redirects": [
  {
    "source": "/source/path",
    "destination": "/destination/path"
  }
]
```

Cela redirige définitivement `/source/path` vers `/destination/path`, afin de ne pas perdre le référencement (SEO) acquis par la page d’origine.

Pour faire correspondre un chemin générique (wildcard), utilisez `*` après un paramètre. Dans cet exemple, `/beta/:slug*` correspond à `/beta/introduction` et le redirige vers `/v2/introduction`.

```json
"redirects": [
  {
    "source": "/beta/:slug*",
    "destination": "/v2/:slug*"
  }
]
```
