---
title: "Configuration d’un monorepo"
description: "Configurez le chemin de la documentation et le répertoire de contenu dans un monorepo afin que Mintlify déploie uniquement votre dossier de docs désigné."
keywords: ["monorepo", "content directory", "multi-project repository"]
---

Configurez Mintlify pour déployer la documentation depuis un répertoire spécifique au sein d’un monorepo. Cette configuration vous permet de conserver la documentation aux côtés de votre code dans des référentiels contenant plusieurs projets ou services.

<Note>
  Un déploiement monorepo utilise un seul dépôt avec un sous-répertoire de docs. Pour combiner le contenu de plusieurs dépôts dans un seul site, consultez [Déploiements multi-référentiels](/fr/deploy/multi-repo).
</Note>

Une structure de monorepo typique pourrait ressembler à ceci :

```text
your-repo/
├── apps/
│   ├── web/
│   └── api/
├── docs/
│   ├── docs.json
│   └── quickstart.mdx
└── package.json
```

Dans cet exemple, vous définiriez le chemin de votre documentation sur `/docs`.

<div id="prerequisites">
  ## Prérequis
</div>

* Accès administrateur à votre projet Mintlify.
* Fichiers de documentation organisés dans un répertoire dédié au sein de votre monorepo.
* Un fichier `docs.json` valide dans votre répertoire de documentation.

<div id="configure-monorepo-deployment">
  ## Configurer le déploiement d’un monorepo
</div>

<Steps>
<Step title="Accéder aux paramètres Git">
Accédez à [Git Settings](https://app.mintlify.com/settings/deployment/git-settings) dans votre Dashboard.

<Frame>
<img
  className="block dark:hidden my-0 pointer-events-none"
  src="/images/monorepo-light.png"
  alt="Le panneau des paramètres du projet dans le menu Git Settings, avec l’option docs.json is in a subdirectory activée et un chemin vers le répertoire /docs renseigné."
/>
<img
  className="hidden dark:block my-0 pointer-events-none"
  src="/images/monorepo-dark.png"
  alt="Le panneau des paramètres du projet dans le menu Git Settings, avec l’option docs.json is in a subdirectory activée et un chemin vers le répertoire /docs renseigné."
/>
</Frame>

</Step>
<Step title="Définir le chemin de votre documentation">
1. Activez l’option **docs.json is in a subdirectory**.
2. Saisissez le chemin relatif vers votre répertoire de documentation. Par exemple, si votre documentation se trouve dans le répertoire `docs`, saisissez `/docs`.
<Note>
  N’incluez pas de barre oblique finale dans le chemin.
</Note>
3. Sélectionnez **Enregistrer les modifications**. L'enregistrement déclenche un nouveau déploiement à partir du chemin configuré.
</Step>
</Steps>