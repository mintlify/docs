---
title: "Configurer l'automerge pour les applications GitHub"
sidebarTitle: "Configurer l'automerge"
description: "Ajoutez une application GitHub à la liste de contournement d'un ensemble de règles pour activer la fusion automatique des workflows."
keywords: ["automerge", "ruleset", "protection de branche", "bypass"]
---

Ajoutez l'application GitHub Mintlify à la liste de contournement d'un ensemble de règles pour que l'application puisse créer et fusionner des pull requests sans être bloquée par les exigences de révision et de vérification de statut. Cela vous permet de configurer des [workflows](/fr/guides/automate-agent) pour fusionner automatiquement les modifications dans `main`.

<div id="prerequisites">
  ## Prérequis
</div>

- Accès administrateur au dépôt
- [Application GitHub Mintlify](/fr/deploy/github) installée sur le dépôt
- Un ensemble de règles existant ciblant la branche `main`

<div id="steps">
  ## Étapes
</div>

<Steps>
  <Step title="Accédez aux ensembles de règles">
    Sur GitHub, allez dans **Settings** -> **Rules** -> **Rulesets** de votre dépôt.
  </Step>
  <Step title="Sélectionnez l'ensemble de règles">
    Cliquez sur l'ensemble de règles qui cible votre branche `main`.

    <Note>
      Si plusieurs ensembles de règles ciblent `main`, l'application doit être dans la liste de contournement de chaque ensemble de règles actif.
    </Note>
  </Step>
  <Step title="Ajoutez l'application GitHub Mintlify à la liste de contournement">
    1. Sous **Bypass list**, cliquez sur **Add bypass**.
    2. Recherchez et sélectionnez l'application GitHub Mintlify.
    3. Définissez le mode de contournement sur **Always**.
  </Step>
  <Step title="Enregistrer">
    Cliquez sur **Save changes**.
  </Step>
</Steps>

<div id="verify-the-setup">
  ## Vérifiez la configuration
</div>

1. **Pour les contributeurs** : Ouvrez une PR ciblant `main`. Confirmez qu'elle nécessite toujours une approbation et des vérifications de statut réussies avant que le bouton de fusion soit activé.
2. **Pour les workflows** : Déclenchez un workflow avec l'automerge activé. Confirmez que le workflow s'exécute et fusionne avec succès une PR.
