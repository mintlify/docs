---
title: "Pages"
description: "Les pages sont les éléments de base de votre documentation"
icon: "letter-text"
keywords: ["tags", "tag"]
---

import IconsOptional from "/snippets/icons-optional.mdx";

Chaque page est un fichier MDX (Markdown étendu avec composants React), qui combine du contenu Markdown et des composants React pour vous permettre de créer une documentation riche et interactive.

## Métadonnées de page

Chaque page commence par un frontmatter, des métadonnées YAML délimitées par `---` au début du fichier. Ces métadonnées définissent l’apparence et le comportement de votre page.

Utilisez les métadonnées pour contrôler :

- Les titres et descriptions de page
- Les titres, icônes et tags de la barre latérale

<ResponseField name="title" type="string" required>
  Le titre de votre page qui apparaît dans la navigation et dans les onglets du navigateur.
</ResponseField>

<ResponseField name="description" type="string">
  Une brève description du contenu de cette page. Elle apparaît sous le titre et améliore le SEO.
</ResponseField>

<ResponseField name="sidebarTitle" type="string">
  Un titre court affiché dans la navigation de la barre latérale.
</ResponseField>

<IconsOptional />

<ResponseField name="tag" type="string">
  Un tag affiché à côté du titre de votre page dans la barre latérale.
</ResponseField>

```yaml Example YAML frontmatter wrap
---
title: "About frontmatter"
description: "Frontmatter is the metadata that controls how your page appears and behaves"
sidebarTitle: "Frontmatter"
icon: "book"
tag: "NEW"
---
```

## Mode de page

Contrôlez l’affichage de votre page avec le paramètre `mode`.

### Par défaut

Si aucun mode n’est défini, la mise en page par défaut affiche une barre latérale de navigation et une table des matières.

```yaml
---
title: "Default page title"
---
```

### Large

Le mode Large masque la table des matières. Utile pour les pages sans titres ou si vous préférez utiliser l’espace horizontal supplémentaire.

```yaml
---
title: "Wide page title"
mode: "wide"
---
```

### Personnalisé

Le mode Personnalisé propose une mise en page minimaliste qui supprime tous les éléments à l’exception de la barre de navigation supérieure. C’est une toile vierge pour créer des pages d’accueil ou toute autre mise en page unique avec un minimum d’éléments de navigation.

```yaml
---
title: "Custom page title"
mode: "custom"
---
```

### Centré

Le mode Centré supprime la barre latérale et la table des matières, et centre le contenu. Utile pour les changelogs ou d’autres pages où vous souhaitez mettre en avant le contenu.

```yaml
---
title: "Center page title"
mode: "center"
---
```

## Pages d’API

Créez des interfaces interactives pour les API (API playground) en ajoutant une spécification d’API dans le frontmatter, via `api` ou `openapi`.

```yaml
---
openapi: "GET /endpoint"
---
```

En savoir plus sur la création de [documentation d’API](/fr/api-playground/overview).

## Liens externes

Créez des liens vers des sites externes directement depuis votre navigation grâce à la métadonnée `url`.

```yaml
---
title: "npm Package"
url: "https://www.npmjs.com/package/mint"
---
```

## Optimisation pour les moteurs de recherche

La plupart des balises méta SEO sont générées automatiquement. Vous pouvez définir manuellement des balises méta SEO pour améliorer le référencement de votre site, le partage sur les réseaux sociaux et la compatibilité des navigateurs.

<Note>
  Les balises méta contenant des deux-points doivent être entourées de guillemets.
</Note>

```yaml
---
"twitter:image": "/images/social-preview.jpg"
---
```

Voir [SEO](/fr/settings/seo) pour l’ensemble des options de métadonnées SEO.

## Mots-clés de recherche internes

Améliorez la visibilité d’une page spécifique dans la recherche intégrée en ajoutant des `keywords` dans vos métadonnées. Ces mots-clés n’apparaîtront ni dans le contenu de la page ni dans les résultats de recherche, mais les utilisateurs qui les saisiront verront la page s’afficher en résultat.

```yaml
---
keywords: ['configuration', 'setup', 'getting started']
---
```
