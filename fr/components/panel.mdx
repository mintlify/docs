---
title: "Panel"
description: "Définissez le contenu du panneau latéral droit"
icon: 'panel-right'
---

Vous pouvez utiliser le composant `<Panel>` pour personnaliser le panneau latéral droit d’une page avec les composants de votre choix.

Si une page contient un composant `<Panel>`, les composants [RequestExample](/fr/components/examples#request-example) et [ResponseExample](/fr/components/examples#response-example) doivent se trouver à l’intérieur de `<Panel>`.

Les composants présents dans un `<Panel>` remplaceront la table des matières de la page.

```mdx
<Panel>
  <Info>Épinglez des informations dans le panneau latéral. Ou ajoutez tout autre composant.</Info>
</Panel>
```

<Panel>
  <Info>Épinglez des infos dans le panneau latéral. Ou ajoutez un autre composant.</Info>
</Panel>
