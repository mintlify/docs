---
title: "Étapes"
description: "Présentez une séquence de contenu avec le composant Steps"
icon: "list-todo"
---

import IconsOptional from "/snippets/icons-optional.mdx";

Utilisez des étapes pour afficher une série d’actions ou d’événements successifs. Vous pouvez ajouter autant d’étapes que nécessaire.

<Steps>
  <Step title="First Step">
    Voici des instructions ou un contenu qui ne concernent que la première étape.
  </Step>
  <Step title="Second Step">
    Voici des instructions ou un contenu qui ne concernent que la deuxième étape.
  </Step>
  <Step title="Third Step">
    Voici des instructions ou un contenu qui ne concernent que la troisième étape.
  </Step>
</Steps>

```mdx Steps example
<Steps>
  <Step title="First Step">
    These are instructions or content that only pertain to the first step.
  </Step>
  <Step title="Second Step">
    These are instructions or content that only pertain to the second step.
  </Step>
  <Step title="Third Step">
    These are instructions or content that only pertain to the third step.
  </Step>
</Steps>
```

## Propriétés de Steps

<ResponseField name="children" type="ReactElement<StepProps>[]" required>
  Une liste de composants `Step`.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  La taille des titres des étapes. Valeurs possibles : `p`, `h2` ou `h3`.
</ResponseField>

## Propriétés individuelles d’une étape

<ResponseField name="children" type="string | ReactNode" required>
  Le contenu d’une étape, soit en texte brut, soit sous forme de composants.
</ResponseField>

<IconsOptional />

<ResponseField name="title" type="string">
  Le titre est le texte principal de l’étape et s’affiche à côté de l’indicateur.
</ResponseField>

<ResponseField name="stepNumber" type="number">
  Le numéro de l’étape.
</ResponseField>

<ResponseField name="titleSize" type="string" default="p">
  La taille des titres d’étape. L’une des valeurs `p`, `h2` ou `h3`.
</ResponseField>
