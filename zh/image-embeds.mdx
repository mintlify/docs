---
title: "图片与嵌入"
description: "添加图片、视频和 iframe"
icon: "image"
---

在文档中添加图片、嵌入视频，并通过 iframe 引入交互式内容。

文档仓库中的所有静态资源都会自动在你的域名下以正确的路径提供。例如，如果仓库中有 `/images/my-logo.png`，该图片可在 `https://docs.yoursite.com/images/my-logo.png` 访问。

<Frame>
  <img className="rounded-xl" src="https://mintlify-assets.b-cdn.net/bigbend.jpg" alt="一张风景照片：前景是紫色花朵，远处是群山，蓝天上点缀着疏散的云。" />
</Frame>

<div id="images">
  ## 插图
</div>

在文档中添加图像，以提供视觉上下文、示例或装饰。

<div id="basic-image-syntax">
  ### 基本图像语法
</div>

使用[Markdown 语法](https://www.markdownguide.org/basic-syntax/#images)向文档添加图像：

```mdx
![描述图像的替代文本](/path/to/image.png)
```

<Tip>
  请务必包含具有描述性的替代文本（alt text），以提升无障碍性和生成引擎优化（GEO）。替代文本应清晰说明图像的内容。
</Tip>

单个图像文件必须小于 20MB。更大的文件请托管到 [Amazon S3](https://aws.amazon.com/s3) 或 [Cloudinary](https://cloudinary.com) 等 CDN 服务。

<div id="html-image-embeds">
  ### 使用 HTML 嵌入图像
</div>

若需更细致地控制图像展示，可使用 HTML `<img>` 标签：

```html
<img 
  src="/images/dashboard.png" 
  alt="主仪表盘界面"
  height="300"
  className="rounded-lg"
/>
```

<div id="disable-zoom-functionality">
  #### 禁用缩放功能
</div>

要禁用图像点击后的默认缩放，请添加 `noZoom` 属性：

```html highlight="4"
<img 
  src="/images/screenshot.png" 
  alt="描述性的替代文本"
  noZoom 
  height="200"
/>
```

<div id="link-images">
  #### 为图像添加链接
</div>

要将图像设为可点击链接，请将其包裹在锚点标签中，并添加 `noZoom` 属性：

```html
<a href="https://mintlify.com" target="_blank">
  <img 
    src="/images/logo.png" 
    alt="Mintlify 标志"
    noZoom 
    height="100"
  />
</a>
```

<Note>
  位于锚点标签内的图像会自动显示指针光标，提示其可点击。
</Note>

<div id="light-and-dark-mode-images">
  #### 适配浅色与深色模式的图像
</div>

要在浅色与深色主题下展示不同图像，请使用 Tailwind CSS 类：

```html
<!-- 浅色模式图像 -->
<img 
  className="block dark:hidden" 
  src="/images/light-mode.png" 
  alt="浅色模式界面"
/>

<!-- 深色模式图像 -->
<img 
  className="hidden dark:block" 
  src="/images/dark-mode.png" 
  alt="深色模式界面"
/>
```

<div id="videos">
  ## 视频
</div>

Mintlify 支持在 Markdown 中使用 [HTML 标签](https://www.markdownguide.org/basic-syntax/#html)，让你灵活创建丰富内容。

<Tip>
  请始终在 video 元素内添加回退文本内容，以便在不支持视频播放的浏览器中显示。
</Tip>

<div id="youtube-embeds">
  ### 嵌入 YouTube
</div>

使用 iframe 元素嵌入 YouTube 视频：

```html
<iframe
  className="w-full aspect-video rounded-xl"
  src="https://www.youtube.com/embed/4KzFe50RQkQ"
  title="YouTube video player"
  frameBorder="0"
  allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
  allowFullScreen
></iframe>
```

<Frame>
  <iframe className="w-full aspect-video rounded-xl" src="https://www.youtube.com/embed/4KzFe50RQkQ" title="YouTube video player" frameBorder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen />
</Frame>

<div id="self-hosted-videos">
  ### 自托管视频
</div>

对于自托管的视频内容，请使用 HTML `<video>` 元素：

```html
<video
  controls
  className="w-full aspect-video rounded-xl"
  src="link-to-your-video.com"
></video>
```

<div id="autoplay-videos">
  ### 自动播放视频
</div>

如需自动播放视频，请使用：

```html
<video
  autoPlay
  muted
  loop
  playsInline
  className="w-full aspect-video rounded-xl"
  src="/videos/demo.mp4"
></video>
```

<Note>
  使用 JSX 语法时，复合词属性应写为驼峰式：`autoPlay`、`playsInline`、`allowFullScreen`。
</Note>

<div id="iframes">
  ## iframes
</div>

使用 iframe 元素嵌入外部内容：

```html
<iframe 
  src="https://example.com/embed" 
  title="Embedded content"
  className="w-full h-96 rounded-xl"
></iframe>
```

<div id="related-resources">
  ## 相关资源
</div>

<Card title="Frame 组件参考" icon="frame" horizontal href="/zh/components/frames">
  了解如何使用 Frame 组件呈现图像。
</Card>
