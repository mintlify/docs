---
title: "配置 GitHub 应用的自动合并"
sidebarTitle: "配置自动合并"
description: "将 GitHub 应用添加到规则集的绕过列表中，以启用工作流程的自动合并。"
keywords: ["automerge", "ruleset", "分支保护", "bypass"]
---

将 Mintlify GitHub 应用添加到规则集的绕过列表中，使该应用能够创建和合并 pull request 而不会被审查和状态检查要求阻止。这允许你配置[工作流程](/zh/guides/automate-agent)以自动将更改合并到 `main`。

<div id="prerequisites">
  ## 前提条件
</div>

- 仓库管理员权限
- 在仓库中安装了 [Mintlify GitHub 应用](/zh/deploy/github)
- 现有的针对 `main` 分支的规则集

<div id="steps">
  ## 步骤
</div>

<Steps>
  <Step title="导航到规则集">
    在 GitHub 上，进入仓库的 **Settings** -> **Rules** -> **Rulesets**。
  </Step>
  <Step title="选择规则集">
    点击针对 `main` 分支的规则集。

    <Note>
      如果多个规则集针对 `main`，该应用必须在每个活动规则集的绕过列表中。
    </Note>
  </Step>
  <Step title="将 Mintlify GitHub 应用添加到绕过列表">
    1. 在 **Bypass list** 下，点击 **Add bypass**。
    2. 搜索并选择 Mintlify GitHub 应用。
    3. 将绕过模式设置为 **Always**。
  </Step>
  <Step title="保存">
    点击 **Save changes**。
  </Step>
</Steps>

<div id="verify-the-setup">
  ## 验证设置
</div>

1. **对于贡献者**：打开一个针对 `main` 的 PR。确认它仍然需要批准和通过状态检查后才能启用合并按钮。
2. **对于工作流程**：触发启用了自动合并的工作流程。确认工作流程运行并成功合并 PR。
