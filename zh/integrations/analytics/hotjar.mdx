---
title: "HotJar"
description: "使用 HotJar 收集用户反馈并生成热力图。"
---

在你的 `docs.json` 文件中添加以下配置，将数据发送到 HotJar 进行 Analytics。

```json Analytics options in docs.json
"integrations": {
    "hotjar": {
        "hjid": "required",
        "hjsv": "required"
    }
}
```
