---
title: "Hightouch"
description: "介绍如何将 Hightouch 集成到 Mintlify 文档站点：在 docs.json 中配置 writeKey 写入密钥与可选的 apiHost 事件主机地址，把文档分析事件同步到 Hightouch，用于受众激活、用户分群和下游营销工具的数据路由分发。"
keywords: ["Hightouch 集成", "数据激活", "write key", "受众同步"]
boost: 3
---

在你的 `docs.json` 文件中添加以下内容，将 Analytics 发送到 Hightouch。

<Info>
  `apiHost` 中不要包含 `https://`。
</Info>

<CodeGroup>
  ```json docs.json 中的 Analytics 选项
  "integrations": {
      "hightouch": {
          "writeKey": "required"
          "apiHost": "optional"
      }
  }
  ```

  ```json 示例
  "integrations": {
      "hightouch": {
          "writeKey": "9132c81do371p76sr11da0075469b54f77649c9a067dp0303p56q0q64n072336"
          "apiHost": "us-east-1.hightouch-events.com" # 可选，默认为 `us-east-1.hightouch-events.com`
      }
  }
  ```
</CodeGroup>