---
title: "Plausible"
---

在 `docs.json` 中添加你站点的域名，以将分析数据发送到 Plausible。

<Info>

    域名或服务器地址中请不要包含 `https://`。

</Info>

<CodeGroup>

```json Analytics options in docs.json
"integrations": {
    "plausible": {
        "domain": "required",
        "server": "optional"
    }
}
```

```json Example
"integrations": {
    "plausible": {
        "domain": "docs.domain.com"
    }
}
```

</CodeGroup>