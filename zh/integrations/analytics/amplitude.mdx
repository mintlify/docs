---
title: "Amplitude"
description: "在 docs.json 的 integrations 中配置 amplitude 的 apiKey，将 Mintlify 文档站点的页面浏览量和用户行为数据发送到 Amplitude，跟踪内容参与指标。本文提供所需的配置字段说明和可直接复制使用的 JSON 示例。"
keywords: ["Amplitude 集成", "Amplitude API key", "用户行为跟踪", "产品分析"]
boost: 3
---

将以下内容添加到你的 `docs.json` 文件中，以将 Analytics 数据发送到 Amplitude。

<CodeGroup>

```json docs.json 中的 Analytics 选项
"integrations": {
    "amplitude": {
        "apiKey": "required"
    }
}
```

```json 示例
"integrations": {
    "amplitude": {
        "apiKey": "76bb138bf3fbf58186XXX00000"
    }
}
```

</CodeGroup>