---
title: "Amplitude"
description: "介绍如何将 Amplitude 集成到 Mintlify 文档站点：在 docs.json 的 integrations 中配置 Amplitude API key，即可自动把页面浏览、搜索查询等分析事件发送到 Amplitude 平台，用于文档的产品分析和用户行为跟踪。"
keywords: ["Amplitude 集成", "Amplitude API key", "用户行为跟踪", "产品分析"]
boost: 3
---

将以下内容添加到你的 `docs.json` 文件中，以将 Analytics 数据发送到 Amplitude。

<CodeGroup>

```json docs.json 中的 Analytics 选项
"integrations": {
    "amplitude": {
        "apiKey": "required"
    }
}
```

```json 示例
"integrations": {
    "amplitude": {
        "apiKey": "76bb138bf3fbf58186XXX00000"
    }
}
```

</CodeGroup>