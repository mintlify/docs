---
title: "LogRocket"
description: "使用 LogRocket 监控用户会话与错误。"
---

将以下内容添加到你的 `docs.json` 文件中，以将 Analytics 数据发送至 LogRocket。

```json Analytics options in docs.json
"integrations": {
    "logrocket": {
        "apiKey": "required"
    }
}
```
