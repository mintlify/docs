---
title: "产品更新"
description: "新版本与改进"
rss: true
noindex: true
---

<Update label="9 月 27 日 - 10 月 3 日" tags={["New releases", "Improvements"]} rss={{ title: "每周更新", description: "Products 导航、无障碍 CLI、API 操作台增强，以及广泛的无障碍改进" }}>
  ## 新功能

  * **Products 导航**：通过产品切换器为多个产品的文档进行组织
  * **无障碍 CLI 命令**：运行 `mint a11y` 测试文档的无障碍问题
  * **API 操作台中的视频响应**：API 操作台现已支持显示视频响应类型
  * **API 操作台预填**：新增预填 API 操作台示例的选项，加快测试速度
  * **未认证反馈**：对启用部分认证的网站，用户现可在未认证情况下提交反馈
  * **Shiki 主题支持**：现已支持使用 Shiki 主题为代码块设定样式
  * **代码块的 Twoslash**：TypeScript 和 JavaScript 代码块现支持 Twoslash 内联类型信息

  ## 无障碍改进

  * 在各组件中新增全面的 ARIA 属性，以更好地支持屏幕阅读器
  * 新增“跳至主要内容”按钮，便于键盘导航
  * 在 Tab 组件中支持方向键导航
  * 增强了工具提示、嵌套列表和代码组的可访问性
  * 改进了整个 UI 的焦点状态与键盘交互模式

  ## AI 助手与 Analytics 增强

  * 为助手 query 分析新增柱状图可视化
  * 在助手分析页统一了日期范围选择器
  * 为助手分析图表添加缩放控件
  * 通过热键与聊天面板改进，优化助手历史记录管理

  ## 缺陷修复与可靠性

  * 修复侧边面板覆盖层的 z-index 问题
  * 修复浅色模式的颜色回退与主题相关样式错误
  * 修复 API 操作台可展开项与锚点链接不兼容的问题
  * 修复面向 ChatGPT 与其他 LLM 代理的 `.md` 链接可访问性
  * 修复按百分比 width 与 height 指定的图片尺寸处理
  * 修复带语言标签与自定义主题的代码块渲染问题
  * 修复折叠面板链接的格式与样式
  * 修复在助手配置为空时页脚间距异常
  * 修复本地存储库图片的卡片 icon 渲染
  * 修复 API 操作台中的音频文件处理（避免 UTF-8 解码）
  * 改进网页版编辑器中的 PR 发布状态管理
</Update>

<Update label="9 月 26 日" tags={["Improvements"]} rss={{ title: "每周更新", description: "语言支持扩展、UI 改进与基础设施增强" }}>
  ## 语言支持扩展

  * 在文档界面中新增对 Romanian 和 Czech 的支持
  * 增强本地化能力，为新语言提供完整翻译覆盖
  * 改进所有主题中的语言选择器功能

  ## UI 与用户体验改进

  * 修复 404 页面上的标签页可见性问题，避免错误的激活标签页高亮
  * 增强图片处理，为未优化图片正确传递 width 与 height 属性
  * 改进 404 页面的布局与样式一致性

  ## 基础设施与性能增强

  * 通过跳过代价高的 Imgix 自动格式化，优化 GIF 图片处理，减少处理时间与带宽占用
  * 通过对动图的专项处理提升图片分发性能

  ## 缺陷修复与可靠性

  * 修复控制台设置中 Assistant 增强功能请求的联系邮箱路由
  * 增强数据库模式更新，以改进用户管理
</Update>

<Update label="9 月 19 日" tags={["Improvements"]} rss={{ title: "每周更新", description: "网页版编辑器与认证改进，以及缺陷修复" }}>
  ## 网页版编辑器与控制台登录改进

  * 持续推进网页版编辑器的 app router 迁移，移除阻塞并提升性能
  * 增强登录错误处理，提供明确的邮箱验证信息
  * 修复邮箱地址验证中的空白字符处理

  ## 认证改进

  * 在登录回调中新增对重定向参数的验证支持，降低登录阻力
  * 改进 SSO 登录流程，优化重定向处理

  ## 缺陷修复与可靠性

  * 修复 GitLab 集成在大型存储库上的问题
  * 通过优化 部署 查询，改进控制台加载性能
  * 修复文档中的 SVG 路径渲染问题
  * 修复搜索与聊天功能中的键盘导航
</Update>

<Update label="8 月 31 日 - 9 月 13 日" tags={["New releases", "Improvements"]} rss={{ title: "每周更新", description: "AI 404 建议、AI 助手网页搜索支持外部来源、安全增强与可靠性修复" }}>
  ## 重大更新

  * **重要增强**：在 404 页面提供 AI 推荐，[当有人访问失效链接 → AI 代理解析路径 → 推荐语义相近的页面](https://x.com/mintlify/status/1966625627773059495)
  * **重大发布**：AI 助手的网页搜索现在可包含外部来源\
    *注意：如需为你的站点启用此功能，请联系我们。*

  ## AI 助手与 MCP

  * 修复了因时间窗口未正确滑动而导致 AI 助手被错误限流的问题
  * 修复了 AI 助手在工具调用中处理空 `text` 块的错误
  * 修复了 MCP 服务器名称与工具调用拼接后有时会超过 MCP 客户端强制的 60 个字符限制的问题
  * 修复了 AI 助手菜单的 height 远大于视口并导致无限滚动的问题
  * 修复了控制台中 AI 助手花费金额可能显示超过两位小数的问题

  ## 网页编辑器与部署

  * 新增安全增强：仅对关联的 Git 托管存储库具有 `write permissions` 的用户才能在编辑器中进行更改
  * 修复了名称中包含 `=` 的 branch 无法创建预览部署的问题
  * 修复了过长的 branch 名称在创建预览部署时会溢出模态框的问题
  * 体验优化：注册邀请中，email query 参数将预填输入框
  * 修复了在 Safari 上通过上下文菜单复制页面无效的问题

  ## API 操作台与导航

  * 多个 API 操作台响应码在聚焦时现以受控的样式化选择菜单显示，而非系统默认选择菜单
  * 你现在可以在 `docs.json` 的导航 groups 中使用 [`expanded` 字段使其默认展开](https://mintlify.com/docs/navigation#default-expanded-state)

  ## SEO 与界面

  * 通过从与各文档站相同的 URL 提供 favicon，修复了搜索引擎中不显示站点图标的问题
  * 修复了 YouTube 嵌入在加载时闪烁的问题
  * 修复了将反馈菜单扩展为包含文字回复时与目录之间出现布局位移的问题
  * 修复了在 Maple 主题中关闭通知横幅后文本溢出到顶栏上方的问题
  * 优化了 Maple 与 Willow 主题，在侧边栏新增登录/登出按钮，便于访问

  ## Analytics 与导出

  * 修复了 AI 助手分析视图与导出的可靠性问题
  * AI 助手分析导出现改为在后台执行，并通过电子邮件发送，以提供更稳定可靠的体验
</Update>

<Update label="August 24 - August 30" tags={["Improvements"]} rss={{ title: "Weekly Updates", description: "反馈收集增强、导航改进与认证相关错误修复" }}>
  ## 重大发布：更强大的反馈收集

  * **重大改进**：读者在选择“点赞/点踩”后，现在可以提供更为详尽的反馈，包括选项与文字评论。你还可以对代码块收集反馈，并在控制台的 Analytics 中查看所有反馈。\
    *注意：请联系我们为你的站点启用此功能。*

  ## 导航与易用性改进

  * 易用性优化：忽略结尾斜杠与双斜杠，你无需在 docs.json 中严格保证格式完全一致
  * 现在可在 `h1-6` HTML 标签上添加 `noAnchor` 属性，避免生成锚点链接
  * Palm 主题现于左下角提供浮动语言选择器，类似 Stripe 的做法
  * 在 docs.json 中新增字段 [`drilldown`](https://mintlify.com/docs/navigation#enable-auto-navigation-for-groups)，用于控制当导航分组展开时，是否自动进入该分组的第一页
  * 易用性优化：嵌套有序列表的样式将在十进制/罗马数字与字母之间交替

  ## 错误修复与可靠性

  * 修复了页面存在 JS 组件时，滚动位置锚点链接工作不正确的问题
  * 修复了因缺少 `x-robots-tag noindex` 头而导致 Google 将原始 `*.md` 文件编入索引的问题
  * 修复了受保护文档的 OAuth 问题：成功完成流程后不会重定向回起始页面
  * 修复了受认证保护文档的预览中无法看到完整导航栏的问题
  * 修复了使用新图片 CDN 处理 SVG 的相关问题

  ## 组件与样式增强

  * 为 `SidebarNavGroupDivider` 新增用于自定义样式的 CSS 选择器
  * 为在 MDX 中定义且含有安全设置的 API 页面新增回归测试，以确保更高稳定性

  ## 性能改进

  * 将 KaTeX CSS 从 cdnjs 迁移到我们在 CloudFront 上的自有 CDN，以降低时延并提升性能
</Update>

<Update label="August 17 - August 23" tags={["Improvements"]} rss={{ title: "Weekly Updates", description: "图像布局修复、认证增强、移动端导航改进与网页编辑器稳定性提升" }}>
  ## 图像处理改进

  * **重大改进**：即使未指定 width 和 height 属性，图像默认也不会再造成布局位移——自动尺寸可防止页面加载过程中的内容跳动
  * 存储库中的所有静态文件（PDF、TXT、XML 等）现会在部署时自动上传并提供服务，实现完整的资源覆盖

  ## 网页编辑器与部署增强

  * 修复了网页编辑器中的 branch 创建流程，现在会正确跳转并停留在新建的 branch 上
  * 增强合并冲突对话框的转义处理，无需刷新页面即可关闭冲突
  * 优化更新流程性能：在部分更新时仅对变更页面进行缓存失效处理，缩短部署时间

  ## 认证与导航改进

  * 新增对自定义子目录的认证支持；若文档服务于 `https://yourdomain.com/docs`，认证将无缝工作
  * 修复了在仅配置一个链接时侧边栏错误显示的问题
  * 全面升级移动端导航：按钮居中并具有合理的外边距/内边距；改进下拉菜单间距；移除空区块的不必要分隔线与边距；修复 Maple 主题中的间隙/内边距问题

  ## 组件与样式修复

  * 解决了将 `<h1-6>` 标签错误转换为 Heading 组件从而破坏自定义样式的问题
  * 在控制台新增一键切换 AI 助手配置，便于管理

  ## 技术改进与可靠性

  * 增强更新工作流的日志系统，加快调试与问题定位
  * 通过从单文件抓取切换为存储库克隆，修复了拥有 10+ OpenAPI/AsyncAPI 规范的客户在 GitHub 上遇到的速率限制问题
  * 提升 AI 助手可靠性：增加备援 LLM 支持、改进限流错误处理，并强化搜索工具功能
</Update>

<Update label="8月10日 - 8月16日" tags={["Improvements"]} rss={{ title: "每周更新", description: "性能优化、LLM 集成、Web 编辑器打磨，以及 API 操作台增强" }}>
  ## 性能与构建优化

  * 在未缓存的 NextJS 无服务器环境中，MDX 转译改为在部署时执行，而非每次页面加载时执行，从而改善未缓存页面的首字节时间（TTFB）。
  * 基于内容的哈希在 MDX 未变化时避免重新转译，将大量页面项目的更新流程时间缩短约 50%（超过 5 分钟的部署大致减半）。
  * 在控制台中预览部署的加载速度更快——我们在后端新增数据库索引并对查询进行并行化。
  * 通过移除每个页面 `rsc` 负载中的重复 `navigation` 数据，减小页面体积——在页面数量多或导航结构复杂时性能提升更明显。
  * 更积极的预取策略，使页面更常实现即刻加载。

  ## API 操作台与 OpenAPI 增强

  * 将 OpenAPI 到 MCP 的转换迁移到后端，使托管的 MCP 服务器能够包含工具（更清晰的文档与配置选项即将推出）。
  * 为 API 操作台新增 Ruby 支持。
  * 新增功能：现在你可以[仅通过 docs.json 指定 API 页面](/zh/api-playground/openapi-setup#auto-populate-api-pages)，无需创建任何新的 mdx 文件。
  * 在来自 OpenAPI 规范的文档导航中支持 [`webhook` 页面](/zh/api-playground/openapi-setup#webhooks)。
  * 优化 AI 模型上下文：在跳转至 Anthropic、OpenAI 或其他提供商时，从 Markdown 链接中去除锚点参数。

  ## Web 编辑器改进

  * 创建/重命名文件时，点击空白处即可保存变更，不再强制要求按 Enter 键。
  * 修复了 branch 导航问题：将 URL 切换到特定 branch 时会被重定向至上次活动的 branch，而非预期目标。
  * 对包含 `/` 的 branch 标题进行正确的 URL 编码，避免导航异常。
  * 修复了 monorepo 控制台编辑器中 `Ctrl+K` 链接快捷键会在前面加上文档仓库路径而导致链接失效的问题。

  ## Analytics 与 LLM 集成

  * 支持自定义 `llms.txt` 与 `llms-full.txt`——将其添加到文档仓库根目录，即可在 `/llms.txt` 与 `/llms-full.txt` 端点提供，用于定制 LLM。
  * 新增 [Hightouch 分析集成](/zh/integrations/analytics/hightouch#hightouch)。
  * 增强了上下文菜单的分析追踪（控制台可视化即将推出）。
  * 为 `llms.txt` 与 `llms-full.txt` 添加端到端（e2e）测试，确保正确提供。

  ## 组件与样式增强

  * `h{1-4}` 标签支持自定义 className，以应用自定义标题样式。
  * 修复了自定义页面模式下 `h{1-4}` 标签被渲染为带徽章的 `Heading` 组件的问题。
  * 为[面包屑](/zh/organize/navigation#breadcrumbs)新增 CSS 选择器，便于自定义 CSS 定位。
  * 通过分析图像尺寸，在 56px height 下保持比例，修复被拉伸的 open-graph 图像。
  * 在上下文菜单中将 `VSCode` 更正为 `VS Code`。
  * 修复了自定义组件内的标题与语义化标题一同出现在目录中的问题。

  ## Bug 修复与稳定性

  * 通过清理导致生成问题的字符，修复了某些页面标题的 PDF 渲染问题。
  * 解决了遇到空的 OpenAPI JSON 文件时出现的命令行界面（CLI）错误 `Cannot convert undefined or null to object`。
  * 修复了自定义 `docs.json` open-graph 元标签被生成标签覆盖的问题。
  * 修复了在访问锚点链接时 RSS 订阅按钮点击失效的问题，现改为使用 origin + pathname 作为 RSS 链接。
  * 通过移除 sourcemaps 提升了命令行界面（CLI）下载速度。

  ## 技术改进

  * 在 CI 流水线中加入可视化测试，以更早发现回归。
  * 增强了错误处理与调试能力。
  * 为新功能与边界情况提供更全面的测试覆盖。
</Update>

<Update label="August 3 - August 9" tags={["Improvements"]} rss={{ title: "Weekly Updates", description: "Search API, OAuth polish, asset serving, and MCP tooling" }}>
  ## 认证改进

  * 组级公开访问：通过 `docs.json` 将整个页面组设为公开，无需在每个页面上设置 `public: true`（[了解更多](https://mintlify.com/docs/authentication-personalization/authentication-setup#group-level)）
  * 在 OAuth 配置中支持 [`logoutURL`](https://mintlify.com/docs/authentication-personalization/authentication-setup#implementation-3)，用于删除上游 cookie 并完成登出
  * 发生 OAuth 错误时，用户会被重定向到你指定的 `logoutURL`，以重新启动认证流程
  * 修复了在 OAuth/JWT 流程中回调前短暂出现 500 错误的问题
  * 在 OAuth/JWT 认证配置中自动去除 URL 中的 `https://`，以防误配置

  ## API 操作台增强

  * 新增 [Search API 端点](https://mintlify.com/docs/api-reference/assistant/search)，可在你的文档之上构建代理和 MCP 服务器
  * 现在会在指定路径提供 `openapi` 和 `asyncapi` 文件（例如 `https://mydocsurl.extension/{openapi-or-file-name}.json`）
  * 现在可以在 openapi 文件中使用 [`x-mint` 字段](https://mintlify.com/docs/api-playground/openapi-setup#x-mint-extension) 来覆盖生成的字段、自定义前言内容，或更改代码示例中的端点 URL
  * 在 OpenAPI 配置中，[`x-mcp` 现为 `x-mint.mcp`](https://mintlify.com/docs/api-playground/openapi-setup#mcp)，用于控制哪些路由以 MCP 工具的形式暴露

  ## AI 助手更新

  * 修复了旧消息的操作菜单（包含复制、点赞等选项）在新消息流式加载时消失的问题
  * 修复了上周[托管 MCP 服务器发布](https://mintlify.com/docs/ai/model-context-protocol#accessing-your-mcp-server)后嵌套的 `/mcp/...` 页面可访问性问题

  ## 性能与可靠性

  * 你文档仓库中的所有图片和视频资源现在都会在你的 domain 上按正确路径提供。例如，如果仓库中有 `/assets/marketing/my-logo.png`，它将可通过 `https://mydocsurl.extension/assets/marketing/my-logo.png` 访问。
  * Mintlify 控制台登录页的邮箱输入框现在会自动聚焦，便于立即输入（易用性优化）
  * 在 Redis 中同时优化了自定义域和子域，以提升导航加载性能（约 50ms 延迟降低）
  * 为 PDF 导出添加了重试逻辑以提升稳定性
  * 修复了在接受或关闭后 Cookie 同意弹窗再次出现的问题——现在会尊重首次选择
  * 通过在 `navigator.write` 中指定 MIME `type`，修复了在 Safari 上将页面复制到剪贴板的问题

  ## 技术改进

  * 修复了在 Windows 和 pnpm 上的 CLI 问题，并增加了 CI 测试以防止回归
  * 改进了错误日志输出——为工程团队调试带来更佳体验
  * 当缺少 `contentDirectory` 文件时，修复了 broken-link CI action 的一些小问题
  * 修复了上周认证保护预览修复引入的回归问题，导致导航 UI 中未正确设置活动标签页
  * 修复了主题的 light 背景色未应用于活动标签页 icon 的问题
  * 修复了在控制台中更改认证类型后先更新又回退到之前已保存类型的问题——现在新选择在保存后会保持
  * 面向使用自定义 UI 库的企业客户的内部 DX 改进——我们现在更容易在更短周期内纳入你的组件并响应需求
</Update>

<Update label="July 27 - August 2" tags={["Improvements"]} rss={{ title: "Weekly Updates", description: "Authentication improvements, search enhancements, and assistant API" }}>
  ## 认证改进

  * 改进本地环境下的认证体验，加快该产品领域的认证功能开发与缺陷修复
  * 现已支持对受认证保护的网站进行预览部署
  * 修复重定向行为，确保用户在认证后正确返回原始页面
  * 修复完整认证状态下的退出按钮显示问题（此前仅在部分认证时有效）

  ## API 操作台增强

  * 修复 API 操作台中的 `multipart/form-data` 文件上传功能
  * 修复锚点链接行为，使点击后仅更新 URL，而不会滚动至页面顶部
  * 修复嵌套选项卡中的锚点链接问题

  ## AI 助手更新

  * 新增 Assistant API，便于集成到你自己的产品中，兼容 AI SDK
  * 聊天回复新增复制按钮
  * 修复助手中重试消息的问题
  * 改进默认助手提示词，使其默认更简洁

  ## 性能与可靠性

  * 通过在输入时中止防抖请求，让搜索更灵敏、更准确
  * 为新的 CDN 预置资源——预计图像资源与页面加载时间将很快得到改善
  * 修复渲染复杂 Mermaid 图（如 GANTT 图）的问题
  * 修复 Windows 上的 CLI 问题以提升稳定性，并新增测试以防止回归

  ## 技术改进

  * 在 NextJS 应用中加入 OpenTelemetry 追踪，以改善客户的首字节时间（TTFB）
  * 从 Octokit 迁移到 GitHub API Client，以降低网页版编辑器体验中的延迟
  * 修复 OpenGraph 的重复 meta 标签
  * 将 MongoDB 从 6 升级至 7，以获得更好的性能和新特性
</Update>

<Update label="July 2025" tags={["New releases", "Improvements"]} rss={{ title: "July Product Updates", description: "Slack app integration, hosted MCP servers, Cursor integration, and performance improvements" }}>
  ## Slack 应用

  * 零阻力访问：机器人会回复私信、@提及，以及你 `#ask-ai` 频道中的任何问题
  * 一键安装：可在数秒内直接从你的 Mintlify 控制台安装
  * 上下文作答：搜索你的全部文档以提供相关且准确的回复
  * 减少支持打扰：将日常问题转化为即时的自助答案

  在我们的 [Slack 应用指南](/zh/ai/slack-app) 中了解更多。

  ## 托管 MCP 服务器

  通过 Mintlify 直接部署托管的 Model Context Protocol (MCP) 服务器，以集成 Claude、Cursor 等 AI 工具。详见我们的 [MCP 指南](/zh/ai/model-context-protocol)。

  通过上下文菜单，帮助用户在文档任一页面快速将你的 MCP 服务器连接到 Cursor 或 VS Code。参见 [Contextual menu](/zh/ai/contextual-menu) 了解更多信息。

  ## 代码块改进

  * 改进语法高亮
  * 新增更多自定义选项，包括专注模式、可展开的代码块、深色与浅色模式自适应、语言下拉菜单、行号和图标
</Update>

<Update label="June 2025" tags={["New releases", "Improvements"]} rss={{ title: "June Product Updates", description: "AI assistant updates and subscribable changelogs" }}>
  ## AI 助手更新

  * 通过具备工具调用的代理式 RAG 提升准确性
  * 提供可导航的引用页面链接，便于用户直接访问答案来源
  * 为助手生成的代码示例提供复制快捷键
  * 在文档的代码块上提供“Ask AI”快捷方式，从助手生成解释

  在 [assistant docs](/zh/ai/assistant) 中了解更多。

  ## 可订阅的更新日志

  * 从更新日志页面自动生成 RSS 源
  * 将支持 RSS 的更新集成到 Slack、电子邮件及其他工具

  在我们的全新 [更新日志指南](/zh/create/changelogs) 中了解更多
</Update>

<Update label="May 2025" tags={["Improvements"]}>
  ## API 操作台稳定性更新

  * 通过搜索查找端点
  * 使用标签标识已废弃的端点
  * 在导航中隐藏自动生成的 API 页面
  * 上传 multipart 或表单数据文件

  了解更多请见 [API 操作台文档](/zh/api-playground/)。

  ## `mint update`

  现在可以使用 `mint update` 来更新你的命令行界面（CLI）。
</Update>

<Update label="April 2025" tags={["New releases", "Improvements"]}>
  ## Web Editor 3.0

  <Frame>
    ![Webeditor3 Jpe](/images/webeditor3.jpeg)
  </Frame>

  全面提升 WYSIWYG（所见即所得）编辑器的易用性。

  **主要改进**

  * 使用 ⌘ + P 快捷键按文件名搜索
  * 页面加载速度提升至 10 倍
  * 搜索 branch 时加载更快
  * 页面选项 Tab 可配置布局、title 和用于 SEO（搜索引擎优化）的 metadata
  * 选中文本时显示浮动工具栏

  **其他修复**

  * 修复更新日志组件的上边距
  * 提升右键操作的可靠性
  * 点击发布后将停留在当前页面，而不会进入空白状态
  * 统一文件 icon 的颜色
  * 连续多次选择新 branch 后的稳定性提升
  * 移除 Diff 模式
  * 通过下拉菜单新建文件夹时行为更一致
  * 修复尝试取消选择时引用块会继续生成更多引用块的问题

  ## AI 翻译（测试版）

  <Frame>
    ![AI Translations graphic](/images/changelog/translations.png)
  </Frame>

  使用 AI 翻译你的全部文档。[了解更多。](zh/navigation#localization)

  ## 导出文档为 PDF（测试版）

  将你的全部文档、某个子目录或单个页面导出为 PDF。

  ## 支持 React hook

  为文档带来交互性。所有标准 React hooks 会在你的 MDX 文件中自动可用。[了解更多。](zh/react-components)
</Update>

<Update label="March 2025" tags={["New releases", "Improvements"]}>
  ## MCP 服务器生成器

  <Frame>
    ![screenshot of MCP server generator](/images/changelog/mcpgenerator.png)
  </Frame>

  生成 MCP 服务器，让 AI 应用可以与你的文档或 API 交互。书面内容会自动生成为 MCP 服务器，你也可以一键从 OpenAPI 规范生成 MCP 服务器。
  查看[MCP 入门文档](/zh/ai/model-context-protocol)。

  ## 改进

  * 为更新日志添加标签，便于终端用户筛选更新
  * AI Chat 支持 Sonnet-3.7。可通过控制台配置你偏好的模型
  * 可在控制台设置中直接修改部署名称

  ## Bug 修复

  * 修复 OG 图片
  * 修复无容器锚点的 icon 样式不一致
  * 改进控制台边框在移动/平板/桌面端的响应式样式细节
  * 在 API 操作台的简洁模式下也显示代码示例
  * Web 编辑器支持 &quot;command + k&quot; 搜索快捷键
  * Callout 内的代码块会扩展以填满 callout 区域的 width
</Update>

<Update label="February 2025" tags={["New releases", "Improvements"]}>
  ## 新的配置架构 `docs.json`

  <Frame>
    ![docs.json 截图](/images/changelog/docsjson.png)
  </Frame>

  我们推出了新的 `docs.json` 架构以替代 `mint.json`，以支持更完善的多层级版本管理、更清晰的视觉理解，以及更加一致的术语。了解变更详情，[查看我们的博客](https://mintlify.com/blog/refactoring-mint-json-into-docs-json)。

  按以下步骤从 `mint.json` 升级到 `docs.json`：

  1. 确保你的命令行界面（CLI）为最新版本

  ```bash
  npm i mint@latest -g
  ```

  1. 在你的文档存储库中运行

  ```bash
  mint upgrade
  ```

  1. 删除旧的 `mint.json` 文件并推送你的更改

  ## CI 检查

  自动对文档进行 lint，查找失效链接、拼写和语法问题，或使用你自己的 Vale 配置来约束写作风格。更多信息见我们的[文档](zh/settings/ci)。

  ## 面向 LLM 的 .md 支持

  现在所有文档页面都会自动提供为纯 Markdown 文件——只需在 URL 末尾追加 `.md`。这便于 LLM 吞吐并处理你文档中的单个页面。

  ## 更多主题

  <Frame>
    ![带有文字 “Themes v2” 的图形](/images/changelog/themes.png)
  </Frame>

  新增[预构建主题](zh/themes)，用于自定义文档的外观与体验。可通过你的[docs.json 文件](zh/settings)进行配置。

  现已提供：

  * Maple
  * Palm
  * Willow

  ## 其他改进

  * [技术写作指南：](https://mintlify.com/guides/introduction)技术文档写作的最佳实践，包括读者研究、内容类型与写作技巧。
  * [Dropdown 组件](zh/navigation#dropdowns)：除 Tabs 和锚点外，还可使用下拉菜单组织导航。
  * [AI 语法修复器](https://x.com/ricardonunez_io/status/1892334887644123192)：网页编辑器会在出现解析错误时检测到，并使用 AI 提出修复建议。
</Update>

<Update label="January 2025" tags={["Improvements"]}>
  ## AI 助手改进

  * 新版 UI，包含专用聊天页面与预填提示
  * 稳定性提升。例如，修复误编辑到错误文件或未编辑任何文件的问题
  * 针对组件添加与编辑的知识更健壮
  * 改进 `docs.json` 文件的编辑体验

  ## 局部认证

  可根据用户权限自定义对任意页面或内容区段的访问。支持接入你自己的认证系统。

  ## 全面改造的 API 操作台

  我们重构了 [API 操作台](/zh/api-playground/) 的设计与性能。更新包括：

  * 更便捷地展开细节以总览字段
  * 更直观的嵌套交互设计，例如添加或删除条目
  * 更快的响应时间

  ## 质量改进

  * 支持对预览部署设置访问认证
</Update>

<Update label="December 2024">
  ## 认证

  <Frame>
    ![认证界面截图](/images/changelog/authentication.png)
  </Frame>

  通过设置 JWT（JSON Web Token）、OAuth 或通用密码让文档仅对授权用户可见。在此隐私防护下，你可以创建内部知识库，或防止竞争对手查看你的文档。
</Update>

<Update label="November 2024">
  ## AI Writer

  <Frame>
    ![AI Assistant](/images/changelog/ai-assistant.jpg)
  </Frame>

  现在你可以在结合所有现有文档的上下文下，请求 AI 对文档进行修改。输入提示后，Writer 会通过生成拉取请求（PR）来提出修改建议。

  ## GitLab 集成升级

  我们改进了与 GitLab 的同步支持，例如启用自动更新和预览部署。查看我们的[GitLab 文档](/zh/deploy/gitlab)以开始使用。

  ## Web Editor

  <Frame>
    ![Web Editor](/images/changelog/webeditor.jpg)
  </Frame>

  我们重构了 Web Editor，现在你可以在与 Markdown 同步的同时，以完整的 WYSIWYG（所见即所得）体验来更新文档。

  查看我们的[Web Editor 入门文档](/zh/editor)。

  ## /llms.txt 支持

  <Frame>
    ![llms.txt support](/images/changelog/llms.jpg)
  </Frame>

  所有文档站点现在都会自动在 /llms.txt 和 /llms-full.txt 提供服务，便于 LLM 轻松摄取你的文档。更多信息请阅读[新的 llms.txt 标准文档](https://llmstxt.org)。

  ## 本地化

  你现在可以对文档进行本地化，其工作方式与版本管理类似。为某个版本添加一个 `locale` 后，Mintlify 中固定的内容（如 “Was this page helpful?”）也会匹配该 locale。

  ### 质量改进

  * 基于用户当前阅读的版本返回聊天与搜索结果
  * 除了 JWT 或 Shared Session 令牌外，新增支持使用 OAuth 对用户进行身份验证
</Update>

<Update label="2024年10月">
  ## 更新日志

  推出全新的 [Update 组件](/zh/components/update)，让你更轻松地向用户展示与告知更新（就像这条一样）。

  <Frame>
    ![更新日志](/images/changelog/changelog.jpg)
  </Frame>

  ## 代码行高亮

  你现在可以在文档中高亮代码行：在语言标识符后添加特殊注释即可突出重点。使用花括号 `{}`，并用逗号分隔指定的行号或范围。

  ```javascript Line Highlighting Example {1,3-5}
  const greeting = "Hello, World!";
  function sayHello() {
    console.log(greeting);
  }
  sayHello();
  ```

  ````mdx
  ```javascript Line Highlighting Example {1,3-5}
  const greeting = "Hello, World!";
  function sayHello() {
    console.log(greeting);
  }
  sayHello();
  ```
  ````

  ## 浅色模式代码块

  代码块现在支持浅色模式，你可以在 `docs.json` 中添加以下配置启用：

  ```json
  "codeBlock": {
    "mode": "auto"
  }
  ```

  ## 高级页脚

  <Frame>
    ![高级页脚](/images/changelog/advanced-footer.gif)
  </Frame>

  你现在可以在标准页脚中添加更多链接。此升级可在落地页与文档之间提供更一致的体验；如果你想突出展示特定页面（如社交媒体或状态页），也能获得更强的自定义能力。

  ## 基于当前用户的搜索过滤

  启用个性化后，搜索结果将基于当前登录用户进行过滤，确保他们只看到相关内容。

  ## AI 聊天的自定义提示

  你现在可以自定义 AI 聊天的提示。如果需要自定义，请联系 [support](mailto:support@mintlify.com)。

  ## 控制台改进

  * 新增在控制台设置中将自定义 domain 直接更改为 /docs 的能力。
  * 合并登录与注册页面，降低阻碍与困惑。
  * 实现发现式登录流程，支持隶属多个组织的用户在组织间切换。
  * 新增使用 Google OAuth 登录。
  * 新增可通过控制台设置添加新的部署。

  ## 错误修复

  * 现在导航中可以使用以斜杠开头的路径。
  * 现在可以在 Web 编辑器中编辑 CSS 和 JS 文件。
  * 修复启用时 `suggestEdit` 仍未显示的问题。
  * 修复搜索与聊天的键盘导航，现在可使用上下箭头键浏览结果。
  * 不允许搜索引擎抓取需要用户认证保护的页面。
  * 当组织被删除时重新验证缓存。
  * 现已使用 Scalar OpenAPI 解析器解析 OpenAPI 定义，从而提升性能、修复解析问题，并提供更清晰的错误信息。
  * 现在在由 OpenAPI 定义自动生成的 API 参考页面中支持顶层说明。
  * 为 icon 添加内联样式支持。
  * 修复文档中自定义 CSS 的闪现问题。
  * 正确在链接中显示行内代码样式。
  * 在浏览器中点击返回按钮时保留滚动位置。
</Update>

<Update label="September 2024">
  ## 自定义字体

  <Frame>
    ![自定义字体](/images/changelog/custom-fonts.jpeg)
  </Frame>

  你可以将文档字体个性化为托管在 CDN 上的自有字体，或从 Google Fonts 中选择，使文档更贴合你的品牌。

  ## Card 组件中的图片

  向 card 添加 `img` 属性，可在卡片顶部显示图片。点击[此处](/zh/components/cards#image-card)了解更多。

  ## 更新速度提升

  <Frame>
    ![性能改进](/images/changelog/performance-improvements.png)
  </Frame>

  对于大型项目（约 3,000 个文件），文档更新的下载步骤现已快约 440 倍，时间减少 99.8%。整体而言，更新期间的文件下载现已快约 5.5 倍，时间减少 81.8%。

  ## SEO 改进

  <Frame>
    ![SEO 改进](/images/changelog/seo-improvements.jpeg)
  </Frame>

  我们修复了文档在移动端和桌面端的布局，使其更符合 SEO（搜索引擎优化）最佳实践——包括为导航栏和切换元素添加合适的 aria 标签。

  ## 控制台改进

  * 控制台完成 App Router 迁移。
  * 控制台中现已提供搜索 Analytics。
  * 控制台新增删除组织功能。
  * 上线 GitLab 连接 UI。
  * 修复了不正确的 Analytics 数据。
  * 现在可以在控制台直接购买附加组件。

  ## Bug 修复

  * 修复了在自定义模式且侧边栏布局为 `sidenav` 时，顶栏不会拉伸至屏幕宽度的错误。
  * 修复了 AI 小部件的相对定位问题。

  ## 更多

  * **API 页面疑难解答**：API 页面可能较为复杂，因此我们整理了
    常见问题，帮助你快速排查 —
    [阅读文档](/zh/api-playground/troubleshooting)
</Update>

<Update label="August 2024">
  ## OpenAPI 参考页面

  * 由 OpenAPI 定义、复杂且递归的端点现在体积缩小了 98%。
  * 我们现在会在 OpenAPI 页面中显示
    [additionalProperties](https://swagger.io/docs/specification/data-models/dictionaries/)。

  ## API 操作台中的文件上传

  默认情况下，API 操作台请求由 Mintlify 代理。现在你可以使用
  `disableProxy` 禁用该行为，以支持诸如文件上传等请求类型。

  * [了解更多 API 配置](zh/settings#api-configurations)

  ## 移动端 SEO 改进

  我们修复了文档的移动端布局，使其更符合 SEO（搜索引擎优化）最佳实践—
  包括为元素添加合适的 aria 标签。

  ## 支持表单

  我们在 Mintlify 控制台中新增了更详细的支持表单。现在你可以
  提交表单与我们联系。

  ## Bug 修复

  * 修复了 Segment 集成功能的一个错误。
  * 与编辑器交互时，我们现在会针对 GitHub 权限给出更细粒度的错误信息。
  * 修复了使用直接链接时导航不会正确展开的问题。
</Update>

<Update label="July 2024">
  ## AI 小部件

  <Frame>
    ![AI 小部件](/images/changelog/widget.png)
  </Frame>

  面向 `Pro` 用户，我们推出了 Mintlify Widget，这是你文档的延伸，
  可在用户提问的任意时间和地点进行解答。你可以将这款
  AI 驱动的聊天机器人添加到任何网页：你的落地页、产品内，或
  现有的文档页面。

  * [阅读博客公告](https://mintlify.com/blog/widget)

  ## Pro 方案

  我们还更新了定价方案，以获得更好的自定义能力和可扩展性。

  * [阅读博客公告](https://mintlify.com/blog/pro-plan)

  ## API 操作台代码示例同步

  当你浏览 API 文档时，所选代码示例现在会在各页面间同步。

  ## 洞察

  目前处于测试阶段，该功能会将常见用户问题与模式
  汇总为易读的报告，并提供由 AI 驱动的产品改进建议。
</Update>

<Update label="June 2024">
  ## 发布周亮点

  * 主题：使用预设主题自定义样式。只需将 Quill、Prism 或 Venus 添加到你的 `docs.json` 文件中，即可更新文档样式。
  * 搜索 V2：可直接检索 OpenAPI 端点的说明和标题以跳转到 API 参考页面，从搜索结果中移除隐藏页面，并享受新版搜索栏 UI。
  * Web 编辑器分支：无需 IDE，可在我们的 Web 编辑器中创建分支。
  * 用户个性化：通过 Shared Session 或 JWT（JSON Web Token）进行用户认证，以便展示个性化内容，例如预填充 API key 或向特定客户展示专属内容。
  * OpenAPI 自动化升级：为自动填充 API 操作台页面，可在 `docs.json` 的 tabs 或 anchors 数组中的对象里添加 `openapi` 字段。
</Update>

<Update label="May 2024">
  ## Okta SSO

  我们现已支持通过 Okta 的 SAML 和 OIDC 登录。

  ## Mintlify REST API

  以编程方式触发你的文档更新。
</Update>

<Update label="April 2024">
  ## 自定义模式

  在 metadata 中添加配置，可移除除顶栏之外的所有元素。
  示例场景：

  * 使用自定义组件创建全局自定义着陆页
  * 添加全屏视频或图片库
  * 嵌入自定义 iFrame 演示元素，提升文档的交互性

  查看我们的[自定义模式文档](zh/pages#custom-mode)。
</Update>

<Update label="March 2024">
  ## 面向 VSCode 的 Mintlify MDX

  无需离开 VSCode，即可插入我们预构建组件与标注的代码片段。[在此安装扩展](https://marketplace.visualstudio.com/items?itemName=mintlify.mintlify-snippets)。
</Update>

<Update label="February 2024">
  ## 质量改进

  * 控制台升级：查看更新日志以了解变更内容和状态，在不同 Mintlify 项目间切换以管理部署
  * 完整支持使用 Tabs 进行版本管理
  * 现已支持通配符重定向
  * 命令行界面（CLI）错误检测：本地开发解析出错时，我们会显示无效 frontmatter 的位置
</Update>

<Update label="January 2024">
  ## 发布周亮点

  * 预览部署：当你创建拉取请求（PR；亦称“合并请求”/Merge Request）时，我们会生成一个唯一链接，展示文档在生产环境中的实时预览。你可以将该链接分享给队友。
  * 片段 V2：现已支持可完全复用的组件与变量。
  * 开源 MDX 引擎：我们开放了两个 API——getCompiledMdx 和 MDXComponent——便于你使用 Mintlify 的 Markdown 与代码语法高亮。[欢迎为该项目贡献代码](https://github.com/mintlify/mdx)。
  * AI 聊天洞察：按日期分段查看聊天历史、在控制台中提升 AI Chat 配额，并查看特定 query 的出现频率。
</Update>