---
title: "图标"
description: "使用主流图标库的图标"
icon: "flag"
---

import IconsRequired from "/snippets/icons-required.mdx";

可使用 Font Awesome、Lucide、SVG、外部 URL，或项目中的本地图文件来增强你的文档。

<Icon icon="flag" size={32} />

```mdx Icon example
<Icon icon="flag" size={32} />
```

## 内联图标

当图标用于段落内时，会以内联方式显示。

<Icon icon="flag" iconType="solid" /> 轻松获得你想要的文档。

```markdown Inline icon example
<Icon icon="flag" iconType="solid" /> 轻松获得你想要的文档。
```

## 属性

<IconsRequired />

<ResponseField name="color" type="string">
  图标颜色，使用十六进制颜色值（例如 `#FF5733`）。
</ResponseField>

<ResponseField name="size" type="number">
  图标大小（以像素为单位）。
</ResponseField>
