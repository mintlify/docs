---
title: 'Panel'
description: '指定右侧面板的内容'
icon: 'panel-right'
---

你可以使用 `<Panel>` 组件，自定义页面右侧面板的内容，放入任意你需要的组件。

如果页面包含 `<Panel>` 组件，则所有 [RequestExample](/zh/zh/components/examples#request-example) 和 [ResponseExample](/zh/zh/components/examples#response-example) 组件都必须置于 `<Panel>` 内。

`<Panel>` 中的组件会取代页面的目录。

```mdx
<Panel>
  <Info>将信息固定到侧边面板，或添加其他任意组件。</Info>
</Panel>
```

<Panel>
  <Info>将信息固定在侧边面板，或添加其他任意组件。</Info>
</Panel>