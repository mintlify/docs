---
title: "可展开项"
description: "使用可展开组件在 API 文档中切换嵌套对象属性的显示，展示请求和响应对象的子字段。本文说明组件的使用方法、关键属性和注意事项，帮助你在 Mintlify 页面中快速应用并持续维护。这些说明适合开发者和文档维护者查阅。你可以结合页面示例完成相关操作。相关做法也便于团队后续维护和协作。"
keywords: ["expandables", "nested properties", "expandable fields", "toggle content"]
boost: 3
---

使用可展开项在响应字段中显示或隐藏嵌套内容。它们对在 API 文档中展示复杂对象属性尤其有用。

<ResponseField name="user" type="User Object">
  <Expandable title="properties">
    <ResponseField name="full_name" type="string">
      用户的全名
    </ResponseField>

    <ResponseField name="is_over_21" type="boolean">
      用户是否已满 21 岁
    </ResponseField>
  </Expandable>
</ResponseField>

```mdx Expandable example
<ResponseField name="user" type="User Object">
  <Expandable title="properties">
    <ResponseField name="full_name" type="string">
      用户的全名
    </ResponseField>

    <ResponseField name="is_over_21" type="boolean">
      用户是否已满 21 岁
    </ResponseField>
  </Expandable>
</ResponseField>
```


<div id="properties">
  ## 属性
</div>

<ResponseField name="title" type="string">
  正在显示的对象名称。
</ResponseField>

<ResponseField name="defaultOpen" type="boolean" default="false">
  设为 `true`，使展开项在页面加载时默认打开。
</ResponseField>