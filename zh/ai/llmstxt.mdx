---
title: "llms.txt"
description: "自动生成 llms.txt 和 llms-full.txt 文件，使 ChatGPT 和 Claude 等 AI 工具能够索引并理解你的文档。"
keywords: ["llms.txt", "LLM 索引编入", "AEO", "GEO", "内容发现"]
boost: 3
---

import { PreviewButton } from "/snippets/previewbutton.jsx"

[llms.txt 文件](https://llmstxt.org) 是一种行业标准，可帮助大语言模型 (LLM) 更高效地索引内容，类似于站点地图对搜索引擎的作用。AI 工具可以使用此文件理解你的文档结构，并定位与用户查询相关的内容。

Mintlify 会在项目根目录自动托管一个 `llms.txt` 文件，列出文档中的所有可用页面。该文件始终保持最新且无需维护。你也可以在项目根目录添加自定义的 `llms.txt` 文件。

<Note>
  根据你的站点配置，认证对 `llms.txt` 和 `llms-full.txt` 的影响有所不同：

  - **完全认证站点**：两个文件都需要认证。无法通过认证的 AI 工具将无法访问这些文件。
  - **部分认证站点**：两个文件均可公开访问，但仅列出公开页面，不包含受用户分组限制的页面。

  有关认证如何影响 AI 功能的更多信息，请参阅[功能可用性](/zh/deploy/authentication-setup#feature-availability)。
</Note>

将 `/llms.txt` 追加到你的文档站点 URL 后查看 `llms.txt`。Mintlify 还会在 `/.well-known/llms.txt` 托管该文件，以兼容采用 `.well-known` 约定的工具。

<PreviewButton href="https://mintlify.com/docs/llms.txt">打开此站点的 llms.txt。</PreviewButton>

Mintlify 会在每个页面的响应中添加 HTTP 标头，以便 AI 工具无需事先知道位置即可发现你的 `llms.txt` 文件以及其他代理资源：

- `Link`：遵循标准 HTTP `Link` 标头格式，用于资源发现。会公布 `llms.txt`、`llms-full.txt`、你的 API 目录、[MCP 服务器卡片](/zh/ai/model-context-protocol#discovery-endpoint)、[代理卡片](/zh/ai/skillmd#agent-card)以及[代理技能索引](/zh/ai/skillmd#skills-discovery-endpoints)。
- `X-Llms-Txt`：一个便捷标头，供检查 `llms.txt` 支持情况的工具使用。

```http Response headers
Link: </llms.txt>; rel="llms-txt", </llms-full.txt>; rel="llms-full-txt", </.well-known/api-catalog>; rel="api-catalog", </.well-known/mcp/server-card.json>; rel="mcp-server-card", </.well-known/agent-card.json>; rel="agent-card", </.well-known/agent-skills/index.json>; rel="agent-skills"
X-Llms-Txt: /llms.txt
```

使用认证的站点还会通过 `rel="oauth-protected-resource"` 公布其 OAuth 受保护资源元数据。如果你在某个基础路径下提供文档，公布的每个路径都会包含该前缀。

<div id="llmstxt-structure">
  ## llms.txt 文件结构
</div>

`llms.txt` 文件是一个纯 Markdown 文件，包含：

* 以 H1 标题形式呈现的**站点标题**。
* **站点描述**，以引用块形式显示在标题下方，来源于 `docs.json` 配置中的 `description` 字段。
* **自定义代理指令**，如果你在 `docs.json` 中设置了 [`markdown.instructions`](/zh/ai/markdown-export#custom-agent-instructions)，则会作为 `Agent Instructions` 块出现在描述之后。
* **结构化内容部分**，包含链接以及文档中每个页面的说明。
* 指向 OpenAPI 和 AsyncAPI 规范的 **API 规范链接** (如果文档中包含这些规范) 。
* **外部链接**放在 `Optional` 部分，用于导航中的任何绝对 URL。

`llms.txt` 文件会按照页面在 `docs.json` 导航中出现的顺序列出。未列入导航但因你设置了 `seo.indexing: "all"` 而被索引的页面会排在最后，并按字母顺序排列。`llms.txt` 文件中的页面链接包含 `.md` 扩展名，以便 AI 工具可以直接获取每个页面的 Markdown 版本。

每个页面的说明来自其 frontmatter 中的 `description` 字段。Mintlify 会使用说明的第一段，并在达到 300 个字符时截断。没有 `description` 字段的页面将在 `llms.txt` 中显示，但不含说明。

`llms.txt` 和 `llms-full.txt` 都会列出你默认语言和默认版本中的页面。它们会排除[隐藏页面](/zh/organize/hidden-pages)以及 frontmatter 中带有 `noindex: true` 的页面，除非你在 `docs.json` 中设置了 `seo.indexing: "all"`。

这种结构化方法使 LLM 能够高效地从宏观层面处理你的文档，并为用户查询定位相关内容，从而提升 AI 辅助文档搜索的准确性和速度。

<Note>
  任何单个 `llms.txt` 索引文件都不能超过 100,000 个字符。当完整索引会超出该预算时，Mintlify 会将其拆分为按范围划分的子索引文件，而不是丢弃页面。所有页面都仍可通过拆分后的索引访问。此限制不适用于 `llms-full.txt`。
</Note>

### 大型站点的按范围索引

当完整索引超出字符预算时，Mintlify 会将你的文档划分为按范围划分的索引：

- 每个按范围划分的索引都提供在 `/_llms/<scope>.md`，其中 `<scope>` 标识其所覆盖的子树。
- Mintlify 首先按导航子树（`docs.json` 中的分组、标签页以及其他结构性容器）进行拆分。对于没有子结构的大型扁平部分，则再按 URL 路径段进行拆分，以使任何单个索引都不超过预算。
- 根 `llms.txt` 文件会新增一个 `## Indexes` 部分，列出可从根直接访问的每个按范围划分的索引，包含面包屑、唯一页面数量以及每个索引的描述。该目录之前会附上一条说明，指示 AI 工具递归地跟随链接，直到到达文档页面。
- 按范围划分的索引与根文件采用相同的格式，因此某个按范围划分的索引本身也可以包含另一个 `## Indexes` 部分。AI 工具应递归遍历这些链接以到达每个页面。

```mdx llms.txt 中 Indexes 部分示例
> Follow every linked index under `/_llms/` recursively until you reach documentation pages.

## Indexes

- [API reference (128 pages)](https://example.com/_llms/api-reference.md): REST endpoints, authentication, and webhooks.
- [Guides / Integrations (42 pages)](https://example.com/_llms/guides/integrations.md): Third-party integration walkthroughs.
```

如果你使用反向代理或维护路径允许列表，请在转发 `/llms.txt` 的同时转发 `/_llms/*`，以便代理能够跟随生成的索引链接。对于托管在基础路径下的文档，改为转发 `<base-path>/_llms/*`。由 Mintlify 管理的自定义域名无需额外配置。有关路由指引，请参见[反向代理](/zh/deploy/reverse-proxy#routing-configuration)。

```mdx Example llms.txt
# 站点标题

> 文档站点的简要描述。

## 文档

- [API](https://example.com/docs/api.md): 端点列表和使用方法
- [安装](https://example.com/docs/install.md): 设置步骤
- [快速开始](https://example.com/docs/start.md): 入门指南

## OpenAPI Specs

- [openapi](https://example.com/docs/openapi.json)

## AsyncAPI Specs

- [asyncapi](https://example.com/docs/asyncapi.yaml)

## Optional

- [Community](https://example.com/community)
```

<div id="llms-fulltxt">
  ## llms-full.txt
</div>

`llms-full.txt` 文件会将你的整个文档站点整合为一个文件，供 AI 工具作为 context 使用，并用于 LLM 索引。每个页面都会以其标题、源 URL、说明以及完整的 Markdown 内容形式呈现。

Mintlify 会在你的项目根目录自动托管一个 `llms-full.txt` 文件。你可以在文档站点的 URL 末尾追加 `/llms-full.txt` 来查看该文件。Mintlify 还会在 `/.well-known/llms-full.txt` 托管该文件，以兼容采用 `.well-known` 约定的工具。

<PreviewButton href="https://mintlify.com/docs/llms-full.txt">打开此站点的 llms-full.txt</PreviewButton>

<div id="custom-files">
  ## 自定义文件
</div>

要添加自定义的 `llms.txt` 或 `llms-full.txt` 文件，请在项目根目录创建同名文件。添加自定义文件会覆盖同名的自动生成文件。如果你删除自定义文件，Mintlify 会恢复使用同名的自动生成文件。

自定义的 `llms.txt` 或 `llms-full.txt` 文件必须包含一个以 H1 标题呈现的站点标题。其他内容为可选。有关可选部分和最佳实践的更多信息，请参阅 `llms.txt` 规范中的[Format](https://llmstxt.org/#format)。
