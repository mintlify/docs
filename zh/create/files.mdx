---
title: "文件"
description: "从文档中提供静态资产。"
keywords: ["静态资产", "支持的文件类型"]
---

Mintlify 会自动从你的文档存储库中，在你的 domain 上的相应路径提供静态资产。例如，如果你的仓库中有 `/images/my-logo.png`，该图片文件可通过 `https://docs.your-project.com/images/my-logo.png` 访问。

你可以向用户提供任何受支持的文件类型，包括 OpenAPI 规范、图片、视频等。

图片大小必须小于 20 MB，其他文件类型必须小于 100 MB。

<Note>
对于启用了认证的文档站点，不支持文件服务。如果你的站点需要认证，静态文件将无法通过其直接 URL 访问。
</Note>

<div id="supported-file-types">
  ## 支持的文件类型
</div>

<div id="image-formats">
  ### 图像格式
</div>

- `.jpeg`, `.jpg`, `.jfif`, `.pjpeg`, `.pjp`
- `.png`
- `.svg`, `.svgz`
- `.ico`
- `.webp`
- `.gif`, `.apng`
- `.avif`
- `.bmp`
- `.tiff`, `.tif`

<div id="video-formats">
  ### 视频格式
</div>

- `.mp4`
- `.webm`
- `.ogg`
- `.avi`
- `.mov`
- `.wmv`
- `.flv`
- `.mkv`
- `.m4v`

<div id="audio-formats">
  ### 音频格式
</div>

- `.mp3`
- `.wav`
- `.flac`
- `.aac`
- `.ogg`
- `.wma`
- `.m4a`

<div id="font-formats">
  ### 字体格式
</div>

- `.woff`
- `.woff2`
- `.ttf`
- `.otf`
- `.eot`

<div id="document-formats">
  ### 文档格式
</div>

<Note>文档格式仅适用于 Custom 订阅计划，且在启用认证时不支持。</Note>

- `.pdf`
- `.txt`
- `.doc`, `.docx`
- `.xls`, `.xlsx`
- `.ppt`, `.pptx`
- `.rtf`
- `.odt`, `.ods`, `.odp`

<div id="data-formats">
  ### 数据格式
</div>

<Note>数据格式仅适用于 Custom 计划，且在启用认证后不支持使用。</Note>

- `.csv`
- `.tsv`
- `.xml`

<div id="archive-formats">
  ### 归档格式
</div>

<Note>归档格式仅适用于 Custom 方案，在启用认证时不支持。</Note>

- `.zip`
- `.rar`
- `.7z`
- `.tar`
- `.gz`
- `.bz2`

<div id="file-organization">
  ## 文件结构
</div>

使用文件夹来组织文件，使你的存储库更易于浏览：

```text
/your-project
  |- docs.json
  |- images/
    |- logo.png
    |- screenshots/
      |- dashboard.png
  |- assets/
    |- whitepaper.pdf
    |- demo-video.mp4
```

文件从你的 domain 根目录提供，因此存储库中的结构会直接映射到 URL 结构。依据前面的示例，`assets/whitepaper.pdf` 可通过 `https://docs.your-project.com/assets/whitepaper.pdf` 访问。
