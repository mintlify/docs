---
title: "获取页面浏览量"
description: "导出 Mintlify 文档的页面浏览量统计，按页面路径查看站点总量和明细，并区分真实用户、AI 爬虫与代理流量。结果支持按日期范围筛选和偏移量分页，适合通过 Analytics API 比较页面表现、识别热门内容、分析访问趋势并优化文档信息架构，为内容团队制定更新计划。"
openapi: /zh/analytics.openapi.json GET /v1/analytics/{projectId}/views
keywords: ["analytics", "浏览量", "页面浏览量", "流量", "导出"]
---

<div id="usage">
  ## 使用方法
</div>

使用此端点导出页面浏览量分析数据。结果包括全站总量和各页面明细，并按人工流量和 AI 流量分类。

使用基于偏移量的分页浏览结果。当 `hasMore` 为 `true` 时，将 `offset` 递增 `limit`。

<div id="filtering">
  ## 筛选
</div>

使用 `dateFrom` 和 `dateTo` 参数按日期范围过滤浏览量数据。

<div id="rate-limits">
  ## 速率限制
</div>

此端点允许每个组织每小时最多 100 次请求。所有分析端点共享此限制。
