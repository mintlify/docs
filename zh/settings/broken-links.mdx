---
title: "重定向与损坏链接"
description: "用于防止无效链接的工具"
icon: "link-2"
---

当你在 docs 文件夹中更改某个文件的路径时，该页面的 URL 也会随之更改。这通常发生在重构文档或修改侧边栏标题时。

<div id="broken-links">
  ## 失效链接
</div>

使用我们的命令行工具检测失效链接。[安装命令行工具](/zh/installation)并运行以下命令：

```bash
mint broken-links
```

命令行工具会识别出文档中不存在的所有相对链接。

<div id="redirects">
  ## 重定向
</div>

在 `docs.json` 文件中添加 `redirects` 字段以设置 301 重定向。

```json
"redirects": [
  {
    "source": "/source/path",
    "destination": "/destination/path"
  }
]
```

这会将 `/source/path` 永久重定向到 `/destination/path`，从而保留原页面既有的 SEO 权重。

要匹配通配符路径，请在参数后添加 `*`。在此示例中，`/beta/:slug*` 会匹配 `/beta/introduction` 并将其重定向到 `/v2/introduction`。

```json
"redirects": [
  {
    "source": "/beta/:slug*",
    "destination": "/v2/:slug*"
  }
]
```
