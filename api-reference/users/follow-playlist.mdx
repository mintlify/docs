---
title: "Follow Playlist"
description: "Add the current user as a follower of a playlist"
api: "PUT https://api.spotify.com/v1/playlists/{playlist_id}/followers"
---

# Follow Playlist

Add the current user as a follower of a playlist

<Note>
This endpoint requires user authentication with the following scope(s): `playlist-modify-public playlist-modify-private`
</Note>


## Endpoint

```
PUT https://api.spotify.com/v1/playlists/{playlist_id}/followers
```

## Request

### Headers

<ParamField header="Authorization" type="string" required>
  Bearer token for authentication: `Bearer {access_token}`
</ParamField>

## Example request

```bash
curl -X PUT "https://api.spotify.com/v1/playlists/{playlist_id}/followers" \
  -H "Authorization: Bearer {access_token}"
```

## Response

Returns JSON with the requested data.

### Example response

```json
{
  // Response data
}
```

## Related endpoints

<CardGroup cols={2}>
  <Card title="API Reference" href="/api-reference/overview">
    View all endpoints
  </Card>
  <Card title="Authentication" href="/getting-started/authentication-flows">
    Learn about authentication
  </Card>
</CardGroup>
