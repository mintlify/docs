---
{}
---

<Note>
  現在レガシーの`mint.json`設定ファイルを使用している場合は、CLIを更新してください：

  ```sh
  npm i -g mintlify@latest
  ```

  そして新しい`upgrade`コマンドをドキュメントリポジトリで実行してください：

  ```sh
  mintlify upgrade
  ```

  これにより`docs.json` に基づいて `mint.json`。その後、`mint.json` ファイルをリポジトリから削除してください。
</Note>
